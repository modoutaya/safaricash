# Deferred Work

## Deferred from: code review of story-3.5 (2026-04-26)

- **No test for `useMembers` returning `isError: true`** — `useCyclesEndingAlert` falls through to `count=0, isLoading=false` when the upstream query errors; the alert silently disappears with no degraded state. Pre-existing pattern across the dashboard placeholder — Story 9.1 will introduce proper dashboard error handling when it ships the full layout. Add an `isError` test case + a degraded UI state at that point. [src/features/cycle/api/useCyclesEndingAlert.ts]
- **Tailwind warning-palette token mismatch (`warning-50/200/800/900` not in config)** — `tailwind.config.ts` defines `warning.{DEFAULT,bg,text,accent,foreground}` but no numeric scale. Existing code in `MemberActionSheet.tsx`, `ProgressiveToast.tsx`, and now `CycleEndingAlert.tsx` references `bg-warning-50 border-warning-200 text-warning-800` — these classes don't compile cleanly to the configured palette. Either add the numeric scale to the config or migrate all usages to `bg-warning-bg text-warning-text border-warning-accent`. Cross-cutting cleanup; out of Story 3.5 scope. [tailwind.config.ts + src/components/domain/MemberActionSheet.tsx, ProgressiveToast.tsx, src/features/cycle/ui/CycleEndingAlert.tsx, src/features/member/ui/MemberList.tsx]

## Deferred from: PRD v1.3 auth pivot (2026-04-21, Story 1.5b)

- **No forced password-change on first login** — at MVP, the founder-generated default password (communicated via WhatsApp) is kept as the collector's permanent password until the collector calls to request a rotation (R-OP1 path). Risk: a password shared in clear on WhatsApp that becomes permanent is a latent leak vector (stolen / resold phone, compromised WhatsApp account). Accepted trade-off for solo-dev MVP — adding a force-change-on-first-login flow adds UX friction (extra screen, validation, state machine) that pilot collectors don't need for 10-person rollout. **Revisit triggers:** (a) first pilot collector reports credential-reuse concern OR (b) before onboarding the first paying collector post-pilot OR (c) when a second operator joins and starts provisioning (at that point introduce a "must change on first login" flow with a temporary-password flag on `public.users`). [Story 1.5b spec AC #13]
- **Re-evaluate SMS OTP for auth when Termii business KYC clears** — the pivot to `signInWithPassword` (PRD v1.3) was driven by Termii's business-KYC requirement (letter of incorporation, RCCM, NINEA) which a solo founder cannot yet provide. Once Termii sender ID is activated (or an equivalent WAEMU SMS provider clears KYC), OTP-on-SIM remains strictly stronger than password on a stolen unlocked phone for the re-auth gate (FR5 — cycle settlement, bulk delete, export). **Revisit triggers:** (a) Termii KYC clears OR (b) before onboarding the first paying collector post-pilot. Action at revisit: restore Story 1.3-style OTP re-auth **for sensitive ops only** (keep password login as the default — it costs zero SMS per session), OR introduce a "trusted device" signed cookie that bypasses OTP for a rolling 7 days. [PRD v1.3 amendment, Story 1.5b spec AC #13]
- **GoTrue phone provider requires a dummy Twilio provider on cloud deploy** — Supabase only flips `GOTRUE_EXTERNAL_PHONE_ENABLED=true` when at least one SMS provider is declared. Local dev is unblocked via dummy Twilio credentials in `supabase/config.toml` (never called because the 1.5b flow never dispatches SMS for auth). For cloud deploy, the operator must toggle on **Dashboard → Auth → Providers → Phone** — which Supabase Cloud typically couples to a real Twilio integration. Workarounds: (a) a Twilio trial account purely to satisfy the provider-configured gate, (b) contact Supabase support about "phone auth without SMS" mode. Revisit when either (i) the first cloud deploy happens OR (ii) Termii KYC clears (at which point proper Termii wiring supersedes this hack). [supabase/config.toml `[auth.sms.twilio]`, README § Enabling the phone provider]

<!-- RESOLVED 2026-04-21 by Story 1.5b (AC #11) — full E2E OTP drive-through no longer applicable; OTP path removed. -->
<!-- [[closed]] Playwright E2E full OTP-UI drive-through still deferred -->

<!-- RESOLVED 2026-04-21 by Story 1.5b (AC #4) — auth-sms-hook Edge Function itself is removed; vault question moot. -->
<!-- [[closed]] Supabase Auth hook secret in vault.secrets -->

## Deferred from: code review of 1-7-sign-out (2026-04-21)

- **`as unknown as AuditRow[]` cast in Deno contract test violates CLAUDE.md "Zod at boundaries"** — `supabase/functions/_shared/emit-session-event.contract.test.ts:~61` bypasses the preferred Zod-parse pattern. Introducing Zod as a Deno dep is out of scope for this review pass. Revisit at the next Deno-side refactor or when a second contract test needs shared shape validation — at that point add a `jsr:@std/...` or `npm:zod` import in a shared `contract-helpers.ts`. [supabase/functions/_shared/emit-session-event.contract.test.ts]
<!-- RESOLVED 2026-04-21 by Story 1.8 (AC 1, 3) — seedCollector fixture lives in tests/e2e/fixtures/seed-collector.ts; flow-5-signout.spec.ts consumes it. -->
<!-- [[closed]] Playwright E2E has no sign-in fixture -->

- **`queryClient.clear()` aborts in-flight mutations silently** — AC6 commits to a simple `clear()` before navigate/toast. A member-create mutation submitted moments before sign-out is dropped without error surfacing. Ops-monitored today; revisit when real users report "disappearing" writes or when offline-sync (Story 8.3) introduces mutation resumption. [src/app/providers.tsx:~93]
- **`audit_log.timestamp` column name is an unquoted Postgres type name** — pre-existing pattern propagated from migration 0001; compiles but fragile against future Postgres or extension shadowing. Revisit at the next audit-log schema touch. [supabase/migrations/20260421000001_emit_session_event.sql, migration 0001]
- **React 18 Strict Mode fires `AuthStateListener` effect twice → dup dev-only toast on SIGNED_OUT** — dev-only; harmless in prod. Revisit if a SIGNED_OUT-deduping pass lands. [src/app/providers.tsx]
- **Idle-timer `setTimeout` can fire after hook unmount and fire a second `requestSignOut("idle")`** — minor; second call hits the RPC with no session → dev-warn. Re-check `activeRef` in `armTimer` callback when next touching the hook. [src/features/auth/api/useIdleTimeout.ts:~93-96]
- **`entity_id = collector_id` sentinel lacks DB CHECK** — rationale documented in AC4 (self-referential sentinel satisfies NOT NULL); a future offline verifier joining `entity_id` without filtering on `entity_table` could collide. Add `check (entity_table <> 'sessions' or entity_id = collector_id)` when the verifier ships. [supabase/migrations/20260421000001_emit_session_event.sql]
- **`signOutStateRef.reason` stays stale if Supabase-js fails to emit SIGNED_OUT** — implementation-dependent on Supabase-js's invariant that SIGNED_OUT fires even on network failure. Revisit if a real incident surfaces where the toast is wrong after a sign-out failure. [src/features/auth/api/signOut.ts]
- **`getSession()` seeding races `onAuthStateChange` subscription** — pre-existing Story 1.5/1.6 pattern; `hadSessionRef` can be false when a SIGNED_OUT event arrives. Out of Story 1.7 scope. Revisit at the next `providers.tsx` refactor. [src/app/providers.tsx]
- **`emit_session_event` RPC has no rate limit** — authenticated caller can append unbounded `session.signed_out` rows to their own chain (serialized by advisory lock but not bounded). Ops-monitored; add a DB-level rate table when abuse is detected. [supabase/migrations/20260421000001_emit_session_event.sql]
- **No schema-drift CHECK between TS `SignOutReason` and SQL `p_reason` whitelist** — TS union `"explicit" | "idle"` vs SQL `in ('explicit','idle')` drift-safe today, no single source of truth. Add a schema-drift test or generate an enum when a third reason is added. [src/features/auth/api/signOut.ts, migration]
- **`emit_session_event` does not REVOKE EXECUTE from `service_role`** — no service_role caller today; service_role calls would fail with "auth.uid() is null" (noisy but not exploitable). Add the REVOKE when the Edge Function story (6.x / 10.x) adds a service-role caller. [supabase/migrations/20260421000001_emit_session_event.sql]
- **`search_path = public, extensions, pg_temp` on SECURITY DEFINER** — all `extensions.*` refs are already schema-qualified (matches `audit_emit` pattern). Minor self-host hardening: drop `extensions` from the search_path when the next security sweep happens. [supabase/migrations/20260421000001_emit_session_event.sql]
<!-- RESOLVED 2026-04-21 by Story 1.8 (AC 3) — upgraded to /vous\s+[eé]tes\s+d[eé]connect[eé]/i in tests/e2e/flow-5-signout.spec.ts. -->
<!-- [[closed]] Playwright regex /vous êtes déconnecté/i is case- but not diacritic-insensitive -->


## Deferred from: code review of 1-6-session-management (2026-04-21)

<!-- RESOLVED 2026-04-21 by Story 1.8 (AC 1, 3) — seedCollector fixture mints session in localStorage before page.goto(); session-idle-timeout.spec.ts consumes it. -->
<!-- [[closed]] E2E test incomplete — no session seeding (1.6) -->

- **`onExpired()` rejection silently swallowed** — `void configRef.current.onExpired()` discards any rejection from `supabase.auth.signOut()`. In practice Supabase clears local session state even on network failure, so functional impact is low. Revisit if a sign-out failure incident surfaces. [`src/features/auth/api/useIdleTimeout.ts:70,163`]

## Deferred from: implementation of 1-5-phone-otp-signin (2026-04-20)

- **Termii API base URL was stale in Story 1.3 constants** — Story 1.3 shipped `TERMII_API_BASE_URL_DEFAULT = https://api.ng.termii.com` (legacy), which returns 404 on `/api/sms/send`. Caught during Story 1.5's first real dispatch because Story 1.3 was never exercised against live Termii (deploy gate). Constant updated to `https://v3.api.termii.com` in `supabase/functions/_shared/constants.ts`. No code review finding — this is a "never reached prod before" class of bug. Revisit: when/if Termii migrates again, prefer reading from Termii's OpenAPI discovery rather than hardcoding. [supabase/functions/_shared/constants.ts]

- **Playwright E2E full OTP-UI drive-through still deferred** — Story 1.8 (2026-04-21) partially closed this by adding a 4th test that verifies the post-authenticated-session landing on `/members` empty-state using the `seedCollector` fixture (session pre-written to localStorage, no OTP UI drive-through). The full "type the 6 digits into `OtpStep`" path remains deferred: reading the OTP out of Supabase `auth.one_time_tokens` is plan-dependent, and supabase-js v2.103.3's `auth.admin.generateLink` does not expose a `type: "sms"` variant. Revisit when EITHER (a) Supabase Auth ships an admin API that returns SMS OTPs, OR (b) a local-only `supabase/config.toml` flag enables deterministic test OTPs, OR (c) Story 1.5's `OtpStep` gets a test-mode hook that accepts a hard-coded 6-digit token when a dev-only env flag is set. Mutation-test verification migrates with it. [tests/e2e/flow-5-login.spec.ts]
- **Supabase Auth hook secret in vault.secrets** — Story 1.5 AC #13 hinted at storing `auth_sms_hook_secret` in `vault.secrets`. At deploy time it's simpler + equally safe to keep it in the Edge Function env (`AUTH_SMS_HOOK_SECRET`) — only operator access can mutate Edge Function secrets, and Supabase Auth posts the same value on every call. Vault integration only matters if a future Story mints hook secrets programmatically (e.g. per-tenant rotation). Revisit when multi-tenant operator provisioning lands. [supabase/functions/auth-sms-hook/README.md]
- **Typed-i18n keys + missing-key compile error** — `src/i18n/keys.ts` infers keys from `fr.json` at TS compile, so missing keys DO surface as type errors — but the inference requires a `resolveJSONModule` import pattern; adding a key via string literal elsewhere (e.g. `t(\`login.foo\`)` template) escapes the check. A dedicated codegen + ESLint rule pair would close the gap. Defer to the first story that adds a second locale (NFR-L2). [src/i18n/keys.ts]

## Deferred from: code review of 1-5-phone-otp-signin (2026-04-20)

- **Contract test comment claims tautological invariant** — `check_collector_registered.contract.test.ts` asserts "no wildcard match" by passing `%` through an `=` predicate. The `=` operator has no wildcards by language design, so the test exercises nothing beyond the language guarantee. Revisit: when someone adds a real injection surface on that RPC. [supabase/functions/_shared/check-collector-registered.contract.test.ts:115]
- **`verifyCode` post-auth count query lacks `AbortSignal` on unmount** — React 18 tolerates stale `setState` on unmounted components silently; no crash today. Revisit when a strict-mode or concurrent-rendering bug surfaces. [src/features/auth/api/useLogin.ts:229-239]
- **Lockout `setTimeout` extends on tab backgrounding** — browser timer throttling means a 5-min lockout could stretch to 10+ min if the tab is backgrounded on battery-save. Fail-closed (safer than shorter); accept until real users complain. [src/features/auth/api/useLogin.ts:131-146]
- **`auth-sms-hook` has no body-size guard** — `req.text()` has no upper bound; Deno Deploy enforces a platform-level ~10 MB cap, so the exploit surface is bounded. Revisit if/when the hook migrates off Deno Deploy. [supabase/functions/auth-sms-hook/index.ts:240]
- **`auth-sms-hook` reads body as text — non-UTF8 bytes silently replaced before HMAC** — in practice Supabase Auth always sends valid UTF-8 JSON, so this is theoretical. Strict defense-in-depth would read as bytes and hash the raw buffer. Revisit if a signature-mismatch incident surfaces with no clock-skew explanation. [supabase/functions/auth-sms-hook/index.ts:240-269]
- **`auth-sms-hook` does not verify `Content-Type: application/json`** — Standard Webhooks spec deviation, no exploit (signature proves Supabase Auth sent it). Revisit at the next webhook-hardening pass. [supabase/functions/auth-sms-hook/index.ts:202-281]
- **`/login` route has no guard redirecting already-authenticated users** — wasted round-trip + minor UX oddity; Story 1.6 (Session management) or 1.7 (Sign-out) own the guard-hardening pass. [src/app/router.tsx:31]
- **Non-registered screen adds uncommanded 🔒 emoji** — spec AC #4 prescribes copy + CTAs + background; the emoji is an added affordance. Revisit at the UX-polish pass or remove if UX spec is tightened. [src/app/routes/non-registered.tsx:22]
- **`AuthStateListener` does not debounce rapid SIGNED_OUT events** — rare stack of duplicate toasts; resolved naturally if the "false toast on cold load" patch is applied (prior-session guard suppresses the dup). Revisit if toast-spam reports emerge. [src/app/providers.tsx:49-54]

## Deferred from: code review of 1-4-rate-limit-middleware (2026-04-20)

- **Cloudflare Health Check + alerting on `ratelimit.middleware_error`** — fail-open is silent today; ops only sees degradation via `wrangler tail`. Re-deferred from Story 1.8 (2026-04-21): Story 1.8's scope covers CI gates, not production observability. New trigger condition: (a) a production rate-limit-bypass-via-KV-failure incident, OR (b) a dedicated Epic-9 observability story. Wiring: Cloudflare Health Check pings the worker URL every 1 min, alert routes to founder Telegram/email when `ratelimit.middleware_error` event-rate exceeds 10/min over 5-min window. [workers/rate-limit/src/index.ts §logJson]
- **Custom domain `api.safaricash.app` instead of `*.workers.dev`** — cosmetic + future-proofing (decouples worker URL from CF account). Defer to a dedicated infra story when DNS provider + SSL termination decisions are made for the production domain.
- **Hash collector_id in operational logs (privacy)** — `collector_id` (Supabase auth UUID, not PII) is logged plaintext. Defense-in-depth: SHA-256 hash before log emit so log dumps don't trivially reveal active collector counts. Defer to log-retention/privacy review (Story 9.x) when CSV-export & data-rights flows force the privacy taxonomy decision. [workers/rate-limit/src/index.ts:117-122]
- **Wire `retry_after_seconds` from 429 body into the i18n `errors.rate_limited` `{seconds}` placeholder** — placeholder added in fr.json but no consumer code reads `retry_after_seconds` from the worker's RFC 7807 body. Re-deferred from Story 1.8 (2026-04-21): this is a consumer-side wiring, not a CI concern. The downstream stories that surface the toast — Story 7.4 (settlement re-auth gate), Story 2.6 (delete-member confirmation), Story 9.3 (CSV export) — own the wiring. Trigger: first story that surfaces a rate-limit toast. [src/i18n/fr.json:3, src/infrastructure/supabase/client.ts]

## Deferred from: code review of 1-3-reauth-edge-function (2026-04-19)

- ~~**PRODUCTION DEPLOY GATE: re-auth Edge Function must NOT be exposed to real production traffic until Story 1.4 (rate-limit middleware) AND Story 1.5 (phone-OTP login) have shipped.**~~ **RESOLVED (2026-04-20) by Stories 1.4 + 1.5.** Both halves now closed: Story 1.4's Cloudflare Worker gates `/functions/v1/*` and Story 1.5 ships the end-to-end login UX. Operational checklist for prod deploy remains valid (see original entry below). The Cloudflare Worker `workers/rate-limit/` enforces 100 req/min/collector via KV sliding window, gating all `/functions/v1/*` calls when the frontend env `VITE_SUPABASE_FUNCTIONS_GATEWAY_URL` is set. Half of the gate is closed: the SMS-bombing risk via re-auth is bounded. The OTHER half (Story 1.5 phone-OTP login flow front-end) remains open — until login lands, there's no real prod traffic flowing through re-auth anyway since users can't sign in. Operational checklist for prod deploy: (1) provision Worker KV namespace via `wrangler kv namespace create RATE_LIMIT_KV`; (2) `wrangler deploy`; (3) set `VITE_SUPABASE_FUNCTIONS_GATEWAY_URL` in Cloudflare Pages prod env; (4) re-deploy Pages; (5) run `tests/e2e/rate-limit.spec.ts` against prod URL to verify 101st req returns 429. Story 1.5 then closes the remaining half by wiring the login UX. [workers/rate-limit/README.md, supabase/functions/re-auth/index.ts]
- **Playwright E2E for re-auth flow** — formally dropped from Story 1.3 (AC #12 amended); the 8 Deno tests provide equivalent contract coverage for the handler. The full UI E2E (modal + OTP input + toast + screenshot) belongs to Story 1.5 (phone-OTP login) where the front-end React surface lands. Mutation-test verification (toggle OTP_MAX_ATTEMPTS=999 → confirm lockout test goes red) also migrates to Story 1.5's spec. [tests/e2e/reauth-flow.spec.ts (file not created), Story 1.5 spec when authored]
- **Forensic actor disambiguation for re-auth events** — `actor='system'` covers the trigger-emitted reauth.* events (Edge Function uses service-role, no JWT context for `current_setting('request.jwt.claim.sub')`). Payload includes `collector_id` so attribution is recoverable, but a real Edge Function actor (vs cron, vs sms-worker) is indistinguishable. Same `app.actor` GUC pattern as Story 1.2's deferred-work entry — revisit together. [supabase/migrations/20260419000008_reauth_challenges.sql, audit_emit() function]
- **Test-process orphans** — `unload` event handler runs best-effort cleanup of seeded auth users + reauth_challenges, but SIGKILL'd test runs leave orphans. Add a periodic janitor or pre-test sweep when pollution becomes measurable (>50 orphan users / day). Pattern: `service.auth.admin.listUsers()` filtered by `email LIKE 'reauth-%@safaricash-test.local' AND created_at < now() - 1 hour`, delete each. [supabase/functions/re-auth/index.test.ts:unload handler]
- ~~**Generic UX missing-key fallback in `useT.ts`**~~ — still raw-key fallback post Story 1.5; typed `TranslationKey` guard catches most drift at compile time, but a runtime fallback (or throw-in-dev) remains nice-to-have. Re-defer to the first story that adds a second locale (NFR-L2). [src/i18n/useT.ts]

## Deferred from: code review of 1-2-supabase-foundation (2026-04-19)

- **Audit-chain gap detection** — `verifyChain` accepts contiguous-but-incomplete chains (an insider with DB write access could DELETE a row + relink the next row's prev_hash and the chain still verifies). Mitigation: per-collector monotonic sequence column on audit_log. Heavy lift; revisit as a dedicated "audit-log integrity hardening" story (likely Epic 1.x or 9.x). [src/domain/audit/verify.ts]
- **Vault row cleanup on member deletion** — DELETE on members leaves orphan `vault.secrets` rows. Story 10.4 (saver-anonymisation Edge Function) owns the cleanup pattern (decrypt-then-delete-secret-then-delete-row). Add to that story's spec.
- **Audit payload contains encrypted UUIDs not plaintext — Vault row swap attack vector** — A DBA who swaps two `vault.secrets` rows leaves the audit chain valid while changing the historical meaning of every audit row referencing those secrets. Mitigations are non-trivial (re-introduce plaintext into another table, OR add a separate `audit_log_secret_witness` table, OR accept). Document in ADR-001 as MVP limitation; revisit if a real attack vector emerges. [docs/ADR/001-supabase-vault.md]
- **Hardcoded event_type CASE in trigger** — Future taxonomy changes (new event types, status-aware mappings beyond `cycle.settled`) require a trigger function rewrite + brief lock. Revisit by extracting to a lookup table when a 3rd new event type lands. [supabase/migrations/20260419000007_triggers_audit.sql]
- **Forensic actor disambiguation** — `actor = 'system'` currently covers trigger writes, Supabase Studio ops, AND any breached service-role usage. Mitigation: future Edge Functions set a session-local GUC `app.actor` and trigger reads it. Defer to Story 1.3 (re-auth Edge Function) where the GUC pattern can be wired alongside other actor metadata.
- **Migration 0004 is dead weight** — only does an index existence check. Reflects spec-mandated 7-file split; cosmetic. No action needed unless the file split is amended in architecture.md.
- **`hashtextextended` advisory lock collision threshold** — At >10k collectors the 32-bit hash space hits ~50% birthday collision probability. Negligible at MVP (<100 collectors). Add a note to `docs/RUNBOOK.md` when it's created (Story 1.1 follow-up) and switch to a 64-bit lock id derived from collector_id when collector count nears 10k. [supabase/migrations/20260419000007_triggers_audit.sql]



Tracks issues raised in code reviews that were judged real but not actionable now. Each entry should be revisited when the trigger condition fires.

## Deferred from: code review of 1-1-project-bootstrap (2026-04-19)

<!-- RESOLVED 2026-04-21 by Story 1.8 (AC 7) — deploy.yml now runs cloudflare/wrangler-action@v3 on PR + push events, gated on vars.CLOUDFLARE_ENABLED. Operator checklist documented in docs/RUNBOOK.md § Cloudflare Pages activation. -->
<!-- [[closed]] Cloudflare Pages preview deploy URL on every PR -->

- **shadcn `cssVariables: false` will break future `npx shadcn add`** — generated components reference `hsl(var(--primary))` which doesn't exist. Fix when first new shadcn component is added: either flip `cssVariables: true` and define `:root` vars in `globals.css`, or post-process the generated component to use Tailwind tokens directly. [components.json]
- **Button `size: sm` h-9 (36 px) violates NFR-A2 44×44 touch target** — only matters when `size=sm` is consumed. Decide in the first story that uses it: either drop the variant or raise to h-11. [src/components/ui/button.tsx]
- **Button uses `rounded-md` (12 px) but UX spec says buttons 14-16 px** — change to `rounded-lg` (16 px) or add a dedicated `rounded-button: 14px` token in the next component-touching story. [src/components/ui/button.tsx]
- **Destructive button `text-white` on `#E24B4A` ≈ 3.0:1 contrast — fails AA body-text** — large-text rule may apply (button is 15 px font-semibold). Audit when destructive button first ships in a real flow (e.g. delete-member confirmation, Story 2.6). Likely fix: use `text-destructive-foreground` (`#712B13`) instead of `text-white`. [src/components/ui/button.tsx]
- **Zod 4 / @hookform/resolvers 5 / RHF 7 cross-version compat unverified** — no forms wired yet. Verify in the first form-using story (Story 1.5 phone-OTP login likely). If incompatible, downgrade Zod to ^3.23 or upgrade resolvers. [package.json]
- **`tsconfig.json` `types: ["vitest/globals", "@testing-library/jest-dom"]` polluting non-test src** — affects IntelliSense; surfaces if production code accidentally imports test globals. Move test types to a tests-only tsconfig or use triple-slash refs in `vitest.setup.ts`. [tsconfig.json]
- **`tsconfig.node.json` not referenced from root `tsconfig.json`** — IntelliSense works but `tsc --noEmit` does not type-check `vite.config.ts` / `vitest.config.ts` / `playwright.config.ts`. Re-deferred from Story 1.8 (2026-04-21): does not affect CI pass/fail today; revisit if a config-file type error escapes CI. [tsconfig.json, tsconfig.node.json]
- **`lint-staged` glob `*.{ts,tsx}` is broader than ESLint's effective surface** — staging `vite.config.ts` triggers a no-op lint pass. Narrow to `src/**/*.{ts,tsx}` and `tests/**/*.{ts,tsx}` at next ESLint config touch. Re-deferred from Story 1.8 (2026-04-21): cosmetic, does not affect any CI gate. [package.json]
<!-- RESOLVED 2026-04-21 by Story 1.8 (AC 8) — @commitlint/cli + config-conventional installed; commitlint.config.cjs + .husky/commit-msg hook + PR-only CI commitlint job all landed. -->
<!-- [[closed]] .husky/commit-msg (conventional-commits validator) missing per architecture tree -->

- **`public/favicon.ico` missing per architecture tree** — `favicon.svg` covers most browsers. Add `.ico` only if a deployment target without SVG support emerges.

## Deferred from: code review of story-6.6-resend-cycle-history (2026-05-12)

- **Concurrent double-submit may duplicate SMS rows** [`supabase/migrations/20260512000004_enqueue_resend_history.sql`] — accepted per AC #9 (no server-side rate limit at MVP; audit log is the abuse surface). Re-evaluate if a real abuse pattern surfaces post-launch.
- **`subsequent_receipt` template used for `advance` rows** [enqueue_resend_history + format_sms_body] — inherited from Story 6.3 trigger pipeline. Fresh advances today send the same wording ("500 FCFA reçu") which is semantically wrong for money-out events. Pre-dates Story 6.6; needs a Story 6.X to introduce an `advance_receipt` template variant.
- **Phone E.164 prepending bug in verify-password.ts** [`supabase/functions/_shared/verify-password.ts:48`] — `+${data.user.phone}` blindly prepends `+`. Phones stored as `00221770000111` would become `+00221770000111` and fail signInWithPassword. Inherited verbatim from Story 1.5b re-auth; now shared across multiple consumers. Validate phone is E.164-shaped before prepending.
- **verify-password collapses "phone lookup failed" vs "no phone on record"** [`supabase/functions/_shared/verify-password.ts:74-85`] — `if (error || !data.user?.phone) return null` loses observability between Supabase Auth infra outage and a legitimate missing-phone case. Pre-existing in re-auth.
- **`error.stack` logged on unexpected errors** [`supabase/functions/_shared/verify-password.ts:138-142`] — supabase-js stack traces can carry URL query strings / internal module paths / partial credentials. Inherited from re-auth.
- **`enqueue_resend_history` P0002 error messages distinguish "doesn't exist" vs "not owned"** [`supabase/migrations/20260512000004_enqueue_resend_history.sql:39-50`] — minor existence-leak via direct PostgREST RPC call. Edge Function hardcodes the detail to `404 not_found` so the leak doesn't surface via the canonical Edge path. Defense-in-depth — uniformize messages or rely on RLS-uniformity.
- **Advisory lock magic `0x5AFA` collision risk** [`supabase/migrations/20260512000002_audit_append_external_extend_resend.sql:62`] — established convention used by `audit_emit` for per-collector chain serialization. Document the magic value in one canonical place (architecture.md § Audit Events).
- **`format_resend_sms_body` worst-case body ~164 chars → 2-segment SMS, not enforced at DB level** — accepted in spec Dev Notes line 200 ("acceptable for low-volume support flow"). Add a CHECK on `sms_queue.body` length if SMS cost becomes a concern.

