# generated: 2026-04-19
# last_updated: 2026-05-15
# touched: 9-2-cycles-ending-dashboard-alerts → done (2026-05-16 — closed as ALREADY-DELIVERED, no implementation. A bmad-create-story gap analysis found that the entire FR35 dashboard cycles-ending-alert surface was built by Story 3.5 [FR20]: CycleEndingAlert + useCyclesEndingAlert [warning-palette banner with count, "Voir" CTA → /members?filter=cycles-ending, "×" → sessionStorage per-session dismiss], mounted on /dashboard, fully E2E-tested by flow-3-cycles-ending-alert.spec.ts. All three Story 9.2 acceptance criteria are satisfied verbatim by that existing code — FR20 and FR35 describe the same dashboard alert. Decision taken with the user: mark 9.2 done directly, no spec / no PR. Below: previous touched line:
# touched (previous): 9-1-dashboard-polled-stats → done (merged 2026-05-16 via PR #73, merge commit 5c8fd85, story commit 5840d22 — FIRST story of Epic 9 [Dashboard & Activity Visibility]. The /dashboard placeholder is replaced with the real morning-glance dashboard: 4 stats [active members / collected today / commission this cycle / 5 recent activities], polled every 60s via TanStack Query refetchInterval, offline-functional from the cached/persisted read-model. Pure deriveDashboardStats module + useDashboardStats hook composing useMembers() with a date-stamped ["dashboard",…] transactions_decrypted query; DashboardStatCards + RecentActivity components; persist filter extended to cold-start the dashboard offline. Cross-LLM code-review applied 6 patches.) Below: previous touched line:
# touched (previous): 9-1-dashboard-polled-stats → done (cross-LLM code review 2026-05-16 via bmad-code-review — claude-sonnet-4-6, 3 parallel adversarial layers on the uncommitted feat/9-1 diff. Verdict: 6 patch + 0 defer + 8 dismissed; all 6 patches batch-applied. HIGHLIGHTS: deriveDashboardStats now sorts recentTransactions newest-first before slicing to 5 [no longer trusts caller ordering — robust to a deserialized persisted cache]; the dashboard query key is date-stamped via a useState lazy initializer so a cold start across a midnight boundary uses a fresh key [the previous-day persisted blob is not rehydrated as "today"]; dashboardTxRowSchema.amount switched to z.coerce.number() [transactions_decrypted.amount is numeric(12,0) — PostgREST may serialise it as a string; matches the project's transactionRowSchema]; staleTime dropped [the refetchInterval owns the 60s cadence — leaving data immediately stale lets a focus/invalidation refresh promptly]; deriveDashboardStats gained a defensive now-param today-filter + a today-boundary unit test [AC #15]; rounded-2xl → rounded-lg [the project's 16px borderRadius token]. 8 DISMISSED: LocalDataNote "always renders" [self-gates on online], E2E offline-dispatch "redundant" [necessary — CDP setOffline does not fire the window event], PERSIST_BUSTER not bumped [correct — the member-query shape is unchanged, the dashboard query populates fresh + self-heals], today no upper-bound [future-dated rows can't occur — server now()], undone-exclusion derivation test [a view invariant — a unit test would be vacuous], E2E one-of-each-kind seed [kind-exclusion arithmetic is exhaustively unit-tested], emoji [cosmetic], LocalDataNote i18n namespace [shared Story-8.6 component]. Gates re-run after the patches: typecheck / lint --max-warnings=0 / 905 vitest passed / build PWA precache 828.90 KiB / full Playwright suite [flow-9-dashboard 2× + all green; flow-3-cycle-settlement + receipt-url-worker fail local-only — wrangler workers not started locally]. Below: previous touched line:
# touched (previous): 9-1-dashboard-polled-stats → review (implemented 2026-05-15 via bmad-dev-story on feat/9-1-dashboard-polled-stats [Node 22 / npm 10] — 8 tasks / 22 ACs; FIRST story of Epic 9 — Dashboard & Activity Visibility. The /dashboard placeholder is replaced with the real morning-glance dashboard: 4 stats [active-members count / collected today FCFA / commission this cycle FCFA / 5 most recent activities]. A pure deriveDashboardStats module does the math [active = displayStatus actif/avance; commission = Σ commission(dailyAmount) via the @/domain/cycle function, INV-4; today = contribution+rattrapage sum, advances excluded, undone excluded by the transactions_decrypted view; recent sliced to 5]. useDashboardStats hook polls every 60s via refetchInterval [architecture Q-ARCH6 — polling NOT Supabase Realtime], composing the already-offline-persisted useMembers() with a ["dashboard","transactions"] query [2 parallel transactions_decrypted fetches — today gte UTC-day-start + recent order-desc limit-5]; it returns members + lastUpdatedAt [dataUpdatedAt — a clock-free time reference so RecentActivity's relative-time labels stay react-hooks-pure]. DashboardStatCards + RecentActivity UI components; the route keeps <CycleEndingAlert /> and adds <LocalDataNote />. The Story-8.6 persist filter shouldPersistMemberQuery is renamed shouldPersistOfflineReadQuery and extended to also persist ["dashboard",…] queries so the dashboard cold-starts offline. Debug: merged a duplicate dashboard i18n key [the existing block had cycles_ending.* for CycleEndingAlert]; fixed an NBSP in a test regex; .first() on a strict-mode E2E locator. Gates: typecheck / lint --max-warnings=0 / 904 vitest passed / branches 75.8% / build PWA 828.68 KiB / full Playwright suite [flow-9-dashboard + flow-3-cycles-ending-alert green; flow-3-cycle-settlement + receipt-url-worker fail local-only — wrangler workers not started locally]. NO new deps / NO migration / NO Realtime — client-side derivation reusing the commission() domain fn. Below: previous touched line:
# touched (previous): 9-1-dashboard-polled-stats → ready-for-dev (spec drafted 2026-05-15 via bmad-create-story; FIRST story of Epic 9 — Dashboard & Activity Visibility [epic-9 → in-progress]. Replaces the /dashboard placeholder with the real morning-glance dashboard: 4 stats [active-members count / amount collected today FCFA / commission earned this cycle FCFA / 5 most recent transaction activities], polled every 60s via TanStack Query refetchInterval [architecture Q-ARCH6 — polling, NOT Supabase Realtime], fully offline-functional. Active-count + commission derive from the existing offline-persisted useMembers(); commission uses the commission() domain fn [INV-4]. Today's-collection + recent-activity from a new ["dashboard",…] transactions_decrypted query, persisted for cold-start offline via an extended Story-8.6 persist filter. Pure deriveDashboardStats module for the math. NO new deps / NO migration / NO Realtime; client-side derivation. 22 ACs / 8 tasks. Below: previous touched line:
# touched (previous): epic-8 → done (2026-05-15 — Epic 8 Offline Resilience COMPLETE, 6/6 stories merged via PR #67-#72. Story 8.6 merged via PR #72 [merge commit fb8d6dc, story commit e2c3ee1]: offline member lookup + edit — TanStack Query persistence for offline reads, useUpdateMember offline branch queueing member.updated events, reconciler member.updated→update_member, migration 0009 update_member p_event_id idempotency, LocalDataNote. The epic ships the full loop: IndexedDB event log → optimistic outbox → deterministic idempotent reconciler replay → stalled-sync alert + manual retry → offline member read/edit. Covers FR40-43 + NFR-P6/P7/R2.) Below: previous touched line:
# touched (previous): 8-6-offline-member-lookup-edit → done (cross-LLM code review 2026-05-15 via bmad-code-review — claude-sonnet-4-6, 3 parallel adversarial layers on the uncommitted feat/8-6 diff [generated database.types.ts + package-lock.json excluded from the review surface]. Verdict: 8 patch + 1 defer + 14 dismissed; all 8 patches batch-applied. HIGHLIGHTS: useUpdateMember now reuses isOfflineAtEntry/getCurrentCollectorId from @/features/transaction/api/offlineGuards [AC #12 + the Safari-private-mode try/catch guard] instead of inline navigator.onLine/getSession; the AC #24 test [useMembers serves the cached/persisted data while offline without erroring] was added; the AC #27 member.deleted-still-skipped reconciler case was added; the E2E gained the "exactly one member.updated audit row" assertion + an offline list-read + search step; onMutate patches the optimistic cache from the PARSED values [the schema trims name + coerces dailyAmount — raw values would diverge from the reconciled truth]; shouldPersistMemberQuery additionally requires state.data !== undefined; the E2E last_event_id assertion switched to toBeTruthy [no vacuous pass on a null row]; providers.test.ts uses the MEMBERS_QUERY_KEY/MEMBER_PROFILE_QUERY_KEY constants. 1 DEFER: members.last_event_id is a single-slot idempotency marker — correct only because the reconciler stops the drain on the first IDB failure [head-of-queue order preserved]; revisit if a retry-first drain policy is ever adopted [deferred-work.md]. 14 DISMISSED: SQL ownership concern [early-return is collector-scoped → falls through to the UPDATE's own guard], inFlightRef-in-mutationFn [pre-existing Story 8.3 pattern, CTA disabled while pending], network-fallback-while-online [spec AC #12 — intended offline-resilience], removeClient void-cast [sync persister no-ops storage errors], timestamp double-conversion [exact Story 8.3 pattern, schema-validated], module-scope supabase [method calls preserve this], role=status+aria-live redundancy [harmless], empty-phone fixture [valid], hard-coded copy [consistent with the file], online-path p_event_id [correct by design], E2E fill auto-wait, navigator.onLine mid-flight toggle [benign], LocalDataNote data guard [already satisfied by placement], audit_log.entity_id column [verified by the psql smoke test]. Gates re-run after the patches: typecheck / lint --max-warnings=0 / 887 vitest passed / branches 76.23% / build / full Playwright suite [flow-8-offline-member 2× + flow-8-stalled-sync + flow-1-offline-replay green; flow-3-cycle-settlement + receipt-url-worker fail local-only — wrangler workers not started locally, unrelated]. EPIC 8 — OFFLINE RESILIENCE — COMPLETE: 6/6 stories. Below: previous touched line:
# touched (previous): 8-6-offline-member-lookup-edit → review (implemented 2026-05-15 via bmad-dev-story on feat/8-6-offline-member-lookup-edit [Node 22 / npm 10] — 9 tasks / 32 ACs; SIXTH and FINAL story of Epic 8 — Offline Resilience. Offline reads served by TanStack Query persistence: PersistQueryClientProvider in providers.tsx with a localStorage sync-storage-persister, a shouldPersistMemberQuery dehydrate filter [member queries only], 24h maxAge + buster; the persisted cache is dropped on sign-out next to queryClient.clear so a second collector cannot rehydrate the first's members. A LocalDataNote [role=status, secondary text] shows "Données locales — synchronisation en attente" on MemberList + MemberProfile when offline. useUpdateMember gains a Story-8.3-style offline branch: networkMode:"always" + an inline navigator.onLine check + a TypeError/network-error fallback → the edit is queued as a member.updated OfflineEvent [new buildMemberUpdateEvent builder], onMutate optimistically patches the MEMBERS_QUERY_KEY + MEMBER_PROFILE_QUERY_KEY caches, onError rolls back, onSuccess invalidation is gated on !wasOffline; the return type became {wasOffline} and the edit route shows an offline toast when queued; a new offline_storage error code is surfaced by MemberForm. The reconciler's resolveRpcName learns member.updated → update_member [member.created/member.deleted stay unsupported — offline create/delete are out of scope]. Migration 0009 adds members.last_event_id + DROP+CREATE update_member with p_event_id + an idempotent early-return [psql smoke test confirmed a replay emits NO second member.updated audit row]; the update-member-idempotent Deno contract test is registered in run-edge-tests.sh. 2 new deps [@tanstack/react-query-persist-client + @tanstack/query-sync-storage-persister — first-party TanStack, NOT a state-management library]. Gates: typecheck / lint --max-warnings=0 / 884 vitest passed [+19 vs 8.5's 865] / branches 76.29% / build PWA precache 824.08 KiB [+7.5 vs 8.5 — the 2 persist deps, within the ≤8KB story budget] / full Playwright suite [flow-8-offline-member + flow-8-stalled-sync + flow-1-offline-replay green; flow-3-cycle-settlement + receipt-url-worker fail local-only — wrangler workers not started locally, unrelated]. CLOSES EPIC 8 — 6/6 stories. Below: previous touched line:
# touched (previous): 8-6-offline-member-lookup-edit → ready-for-dev (spec drafted 2026-05-15 via bmad-create-story; SIXTH/FINAL story of Epic 8. Extends the offline write loop to the member surface. User decisions: offline reads via TanStack Query persistence [not the architecture's SW NetworkFirst — untestable in dev/E2E]; scope = read + edit-update only [create/delete/import out of scope]. useUpdateMember gets a Story-8.3-style offline branch; reconciler learns member.updated→update_member; migration adds members.last_event_id + p_event_id to update_member; LocalDataNote shows "Données locales — synchronisation en attente". 2 new TanStack persist deps, 1 migration, 32 ACs / 9 tasks. Closes Epic 8.) Below: previous touched line:
# touched (previous): 8-5-stalled-sync-alert → done (merged 2026-05-15 via PR #71, merge commit 1873e9a, story commit edf8b08 — Epic 8 Story 5/6: stalled-sync alert + manual retry [FR43 / NFR-P7]. New useStalledSync hook [time-based 15-min stalled detection, reload-durable per-collector localStorage marker], useConnectivityState.hasFailed wired off the Story 8.1 placeholder, ConnectivitySyncDrawer body filled [pending-ops list + role=status stalled banner + "Retenter" → replayPendingEvents], reduced-motion-guarded pill pulse, 15 new i18n keys, Playwright flow-8-stalled-sync E2E. Cross-LLM code-review applied 10 patches + 1 defer. Below: previous touched line:
# touched (previous): 8-5-stalled-sync-alert → done (cross-LLM code review 2026-05-15 via bmad-code-review — claude-sonnet-4-6, 3 parallel adversarial layers [Blind Hunter / Edge Case Hunter / Acceptance Auditor] on the uncommitted feat/8-5-stalled-sync-alert diff. Verdict: 10 patch + 1 defer + 7 dismissed; all 10 patches batch-applied. HIGHLIGHTS: retrySucceeded now requires networkFailures===0 && skipped===0 so a partial drain no longer shows the success banner [AC #14]; retryResult/events/loadedAt reset on drawer close so no stale hint leaks to the next open; AC #22 — a real @axe-core/playwright scan added to the E2E via the shared fixtures/axe.ts expectNoA11yViolations helper, and the dev-record's false "no E2E uses @axe-core/playwright" claim corrected; resolveThresholdMs hardened to reject a stray "0" override [parsed > 0]; recordedLabel guards loadedAt===0; drawer empty-state gated on events.length not just the pendingCount prop [AC #13]; the Story 8.1 outline-Fermer-button test [dropped in the rewrite] re-added [AC #5]; a retry-drains-queue → empty-state test added [AC #25]; the Edge E6 "move the threshold setItem after reconnection" fix was investigated + REJECTED — it breaks the test because useStalledSync reads the threshold once per effect run [not reactively from localStorage] — instead the genuine E2E flake [page.unroute racing the reconciler/manual retries] was fixed with a mutable blockReplay route flag, verified 3× green; useStalledSync "initial pendingCount 0" test gained the result.current assertion. 1 DEFER: ConnectivitySyncDrawer.handleRetry has no cancellation guard for a mid-retry collector change [sign-out during a drain] — low-probability, filed in deferred-work.md. 7 DISMISSED: flip-timer stale-closure [cleanup correct], flush() mechanism coupling, kindKey default branch [defensive dead path], memberName getQueryData no cache-subscription [spec-mandated by AC #11], E2E count===2 [false positive — verified vs flow-1-offline-replay], i18n {x} interpolation [project syntax], row_recorded_at key name [spec permits dev adjustment]. Gates re-run after the patches: typecheck / lint --max-warnings=0 / 865 vitest passed / branches 76.13% / build / full Playwright suite [flow-8-stalled-sync + flow-1-offline-replay green; flow-3-cycle-settlement + receipt-url-worker fail local-only — wrangler workers not started locally, unrelated]. Below: previous touched line:
# touched (previous): 8-5-stalled-sync-alert → review (implemented 2026-05-15 via bmad-dev-story on feat/8-5-stalled-sync-alert [Node 22 / npm 10] — 8 tasks / 30 ACs. New useStalledSync hook: time-based NFR-P7 15-min stalled detection anchored to ONE reload-durable per-collector localStorage marker [safaricash:sync:stalled-since:{collectorId}], written when the outbox first goes non-empty, cleared only on an observed >0→0 drain or a collector switch — NOT on the transient initial pendingCount=0 [count-still-loading], NOT reset by going offline; all setStalled dispatched from setTimeout callbacks so react-hooks/purity + set-state-in-effect hold [Date.now only in the effect body]; STALLED_THRESHOLD_MS exported + a safaricash:e2e:stalled-threshold-ms localStorage override seam for the E2E. useConnectivityState.hasFailed wired to useStalledSync — the Story 8.1 hardcoded `false` placeholder is gone; deriveState untouched [offline still wins over a would-be-stalled queue]. ConnectivitySyncDrawer skeleton body filled: `state` prop reinstated, pending-operations list by member [listEvents + member names from the MEMBERS_QUERY_KEY cache, neutral fallback], amber role=status [NOT role=alert] stalled banner in sync-failed, calm syncing hint in syncing, "Retenter" CTA → replayPendingEvents(collectorId) with disabled+spinner in-flight + session-expired re-auth hint; App.tsx passes connectivity.state. ConnectivityIndicator pulse + spin now motion-reduce:animate-none guarded. 15 new connectivity.drawer.* i18n keys [placeholder_pending removed]. Tests: 13 useStalledSync + 3 useConnectivityState wiring + 11 ConnectivitySyncDrawer [jest-axe in syncing+sync-failed] + 1 ConnectivityIndicator motion assert + Playwright flow-8-stalled-sync [offline write → blocked replay → sync-failed pill → drawer banner+Retenter → unblock → drain → server row + connected]. Gates: typecheck / lint --max-warnings=0 / 863 vitest passed [+19 vs 8.4 baseline 844] / branches 76.12% global [≥75%] + useStalledSync 93.75% isolated [≥85%] / build PWA precache 816.61 KiB / full Playwright suite run locally: flow-8 + flow-1-offline-replay + Flow 1/2/5/6 + rls-isolation green; 2 failures flow-3-cycle-settlement + receipt-url-worker are local-env-only [wrangler workers not started locally — CI starts them; identical to the 8.4 local run; unrelated to this connectivity-only story]. NO new deps / NO migration / NO Edge Function / NO src/domain change / NO OfflineEvent mutation [the IndexedDB event log stays append-only]. Below: previous touched line:
# touched (previous): 8-5-stalled-sync-alert → ready-for-dev (spec drafted 2026-05-15 via bmad-create-story; FIFTH story of Epic 8 — Offline Resilience; makes the silent offline-write loop of Stories 8.2-8.4 visible + actionable on failure. Time-based stalled detection: an event pending > 15 min after reconnection (NFR-P7 threshold STALLED_THRESHOLD_MS) is classified stalled, via a new useStalledSync hook backed by ONE reload-durable localStorage marker per collector — the IndexedDB event log stays IMMUTABLE, no OfflineEvent field added (eventLog.ts header mandates this). Wires useConnectivityState's hasFailed placeholder [hardcoded false since Story 8.1] to the real stalled predicate so the pill escalates to the existing sync-failed state — deriveState is NOT touched. Fills the ConnectivitySyncDrawer skeleton body [Story 8.1 shipped title+close+placeholder] with the pending-operations-by-member list [listEvents + member names resolved from the MEMBERS_QUERY_KEY cache, no network] + a "Retenter" CTA that re-invokes Story 8.4's replayPendingEvents(collectorId) with the current auth session [the BDD's "fresh context"]. Subtle amber pulse on the stalled pill, prefers-reduced-motion guarded. ConnectivitySyncDrawer `state` prop reinstated [Story 8.4 had removed it pending a consumer]. Stalled banner uses role=status NOT role=alert per the UX "never a red alarm" invariant. New connectivity.drawer.* i18n keys. STALLED_THRESHOLD_MS is test-seam-overridable via localStorage key safaricash:e2e:stalled-threshold-ms so the Playwright E2E need not wait 15 real minutes. NO new deps / NO migration / NO Edge Function / NO src/domain change; bundle ≤ 3 KB gzipped. 30 ACs / 8 tasks. Locks closure for Story 8.6 [offline read path — final Epic 8 story]. Below: previous touched line:
# touched (previous): 8-4-reconciler-replay → done (merged 2026-05-15 via PR #70, merge commit 44e46e2, commits a1a6d6a [story] + 948e12c [CI fix — 42804 source enum cast] + 1182cef [CI fix — TanStack networkMode 'always']; CI took 3 pushes + 1 re-run: push 1 failed 5 Playwright E2E with SQLSTATE 42804 datatype_mismatch — the new `CASE WHEN p_event_id IS NULL THEN 'online' ELSE 'offline_reconciled' END` returns text, the source column is transactions_source_enum, and Postgres does NOT auto-coerce a CASE result the way it coerces a bare string literal — fix migration 0061 DROP+CREATE the 3 record-* RPCs with an explicit ::transactions_source_enum cast, lesson saved to memory feedback_migration_rpc_smoke_test.md; push 2 failed only flow-1-offline-replay E2E — TanStack Query's default networkMode 'online' PAUSES mutationFn while navigator.onLine===false, so the in-hook offline branch was unreachable dead code and mutateAsync hung indefinitely; mocked-guard unit tests passed anyway because jsdom navigator.onLine defaults true — fix networkMode 'always' on all 3 record-* hooks, lesson saved to memory feedback_tanstack_networkmode_offline.md; push 3 failed only a transient vault_encrypt 502 flake in promote-cycle-on-advance.contract.test.ts [pre-existing Deno test, unrelated to this story] — cleared on a job re-run with no code change. Implementation:
# touched (previous): 8-4-reconciler-replay → done (implemented 2026-05-15 via bmad-dev-story + cross-LLM code-review on sonnet-4-6 [Blind Hunter / Edge Case Hunter / Acceptance Auditor 3-layer adversarial] — verdict Changes requested, 21 patches all applied in batch [8 HIGH + 9 MED + 4 LOW]: HIGH fixes were event_id UNIQUE index re-partitioned by (collector_id, event_id) via new migration 0060 [matches AC #20 fresh-INSERT semantics + classifyReplayError adds 23505→unique_violation for cross-tab race self-healing], makeContribEvent fixture UUID divergence eliminated [shared between eventId+entityId+p_event_id — tests had been vacuous], stopReplay resets stopRequested flag when no in-flight, useReconciler adds backoff timer via computeBackoffMs+setTimeout chain [previously missing entirely — only `online` event triggered retry], useReconciler adds .catch for unhandled rejection on listEvents IDB failure, useReconciler resets triggeredRef on every effect run [cross-collector sign-in/out], replayPendingEvents tracks inFlightCollectorId + queues cross-collector behind in-flight, scripts/run-edge-tests.sh now lists record-rpcs-idempotent.contract.test.ts [was MISSING — test:edge gate was vacuous], reconciler console.warn for unsupported_kind, ReplayResult.sessionFailures separated from networkFailures [42501/28000 unauthorized had been miscounted]. MED: classifyReplayError typeof object guard before unsafe cast, useReconciler invalidation gated on succeeded>0 && all-failures==0, NFR-P6 test 30→150 events (full spec compliance), deleteEvent failure after RPC success → transient_idb classification → retries on next drain, Playwright explicit window.dispatchEvent("online") after setOffline(false), spec body amended for AC #6 + AC #20 (combined contract test file + cross-collector fresh-INSERT semantics), _resetReconcilerForTests awaits orphan drain, record_advance capacity-at-replay edge case documented. LOW: inline comment 12→17 scenarios, audit_log accessibility assert, drainInternal mid-drain snapshot limitation in header docs. Defers (3): useReconciler unmount-remount benign re-fire / DROP FUNCTION race during live deploy / E2E 15s poll on slow CI — all filed in deferred-work.md. Dismissed (1): supabase.rpc payload cast no Zod parse (acceptable). Gates: typecheck / lint --max-warnings=0 / 844 vitest passed (NFR-P6 functional test now runs 150 events) / branches 75.77% global (≥ 75% gate) / build PWA precache 811.81 KiB / gzipped JS 227.2 KiB (+1.14 KiB vs Story 8.3 baseline 226.0 → AC #24 ≤ 3 KB gzipped ✓). New migration 0060 re-partitions UNIQUE index by (collector_id, event_id); new ReplayResult.sessionFailures field. Implementation:
# touched (previous): 8-4-reconciler-replay → review (implemented 2026-05-15 via bmad-dev-story on feat/8-4-reconciler-replay [Node 22 / npm 10] — closes the offline-write loop opened by Stories 8.2/8.3): 4 migrations [0056 event_id UUID col + partial UNIQUE idx WHERE event_id IS NOT NULL + transactions_decrypted view re-derived, 0057/0058/0059 DROP+CREATE 3 record-* RPCs with p_event_id UUID DEFAULT NULL last param + idempotent early-return SELECT id WHERE event_id=p_event_id AND collector_id=auth.uid() → RETURN if found, source flips to 'offline_reconciled' when p_event_id provided]; reconciler.ts under src/infrastructure/sync/ ~220 LOC exporting replayPendingEvents + stopReplay + classifyReplayError + _resetReconcilerForTests; single-in-flight via module-scope `inFlight` promise (concurrent calls share Promise — verified Object.is identity); serial drain loop timestamp-ASC + eventId tiebreak (NO Promise.all batching — order matters for future member.* events); per-event dispatch via resolveRpcName(eventType); error classification: TypeError/5xx=network drain-stops / 42501+28000=unauthorized drain-stops / 23514+22023+22000=validation skip+continue / P0002+PGRST116=not_found skip+continue / unsupported_kind=skip+continue (transaction.undone + member.* belong to future stories); useReconciler hook ~50 LOC subscribes window `online` event + mount-once boot replay (React 18 Strict Mode double-fire guarded via triggeredRef) + invalidates MEMBERS_QUERY_KEY + MEMBER_PROFILE_QUERY_KEY on clean drain (succeeded > 0 && networkFailures == 0); backoff.ts TypeScript port of SMS-worker exponential schedule [10,30,60,120,300,600]s cap 600s (computeBackoffMs); mounted in AppLayout alongside useConnectivityState; 33 net new vitest cases (5 backoff + 17 reconciler [happy 2 + error classification 5 + concurrency 2 + unsupported 2 + 30-event NFR-P6 functional + classifyReplayError 5] + 6 useReconciler [mount triggers + null-collector no-call + online re-fires + invalidate on clean + no-invalidate on networkFailures + unmount removes listener]); 9 Deno contract tests (3 RPCs × insert + idempotent-replay returns same id + cross-collector 23505 unique_violation) — note: spec AC #20 said cross-collector should "fall through to fresh INSERT" but actual behavior is 23505 UNIQUE violation because the partial index is system-wide (event_id is a global uniqueness contract, not collector-scoped — this is CORRECT semantics: one event = one transaction; collectors never share event_id namespaces in practice as they're client-generated UUIDs); Playwright flow-1-offline-replay E2E: offline → record → IDB stores → pill counts up → online → reconciler drains → server-side row with source=offline_reconciled + audit transaction.committed lands; debug log: db:types CLI prints to stdout (had to redirect stderr to /dev/null) + reconciler test fixture timestamp generator overflowed for idx≥10 (fixed via String(idx%60).padStart(2,"0")) + explicit re-export of OfflineEvent type for future consumers; gates: typecheck / lint --max-warnings=0 / 844 vitest passed (+28 vs Story 8.3 baseline of 816) / branches 75.81% global (≥ 75% gate) / build PWA precache 810.75 KiB / gzipped JS 226.7 KiB (+0.7 KiB vs 8.3 baseline of 226.0 KiB → AC #24 ≤ 3 KB ✓); NO new deps (reconciler uses existing supabase-js rpc, backoff is pure TS); Story 8.1's `hasFailed = false` placeholder STAYS untouched (verified via grep — Story 8.5 will surface ReplayResult.skipped events as the source); memory hooks applied (Node 22 nvm + transactions_decrypted view re-derived per project_views_after_columns.md + supabase.rpc inline call + DROP+CREATE for SQLSTATE 42P13 + db:migrate not db:reset); locks closure for Story 8.5 (stalled retry UI consumes ReplayResult.skipped + sets hasFailed) / 8.6 (offline read path reuses reconciler trigger surface). Below: previous touched line:
# touched (previous): 8-4-reconciler-replay → ready-for-dev (spec drafted 2026-05-15 via bmad-create-story; FOURTH story of Epic 8; closes the offline-write loop: 4 migrations [event_id UUID col + partial UNIQUE idx + transactions_decrypted view extension + DROP+CREATE 3 record-* RPCs with p_event_id idempotent early-return] + reconciler module [single-in-flight, serial timestamp-ASC drain with per-event error classification: network=stop+backoff / unauthorized=stop / validation=skip+continue] + exponential backoff 10s→600s mirroring SMS-worker NFR-R4 + useReconciler hook [online event + mount-once boot + queryClient.invalidateQueries on success]; NFR-P6 budget met serially (~300ms/event × 150 = 45s typical, half 90s budget); 12 reconciler vitest + 4 useReconciler + 9 Deno contract (3 RPCs × insert/idempotent-hit/cross-collector) + Playwright flow-1-offline-replay E2E; NO new deps; memory hooks applied (Node 22, transactions_decrypted view update, supabase.rpc binding preservation, BroadcastChannel poll-with-deadline in tests, SQLSTATE 42P13 DROP+CREATE workaround, db:migrate not db:reset); Story 8.1's hasFailed=false STAYS (Story 8.5 surfaces ReplayResult.skipped as the source); locks closure for Story 8.5 + 8.6. Below: previous touched line:
# touched (previous): 8-3-outbox-pattern-queue → done (merged 2026-05-15 via PR #69, commits 817b3d5 [story] + e07391a [CI fix — flaky BroadcastChannel timing]; CI green on 2nd push — 1st push failed `eventLog.test.ts > appendEvent posts { type: 'append' } on success` with `expected [] to have a length of 1 but got +0` because `flushChannel()` used setTimeout(0) + microtask which races BroadcastChannel dispatch under CI load; fix: poll-with-deadline pattern `waitForMessages(spy, expected, 500ms)` looping every 5ms with a generous deadline; lesson saved to memory `feedback_broadcastchannel_test_timing.md`. Implementation:
# touched (previous): 8-3-outbox-pattern-queue → done (implemented 2026-05-14 via bmad-dev-story + cross-LLM code-review on sonnet-4-6 [Blind Hunter / Edge Case Hunter / Acceptance Auditor 3-layer adversarial] — verdict Changes requested, 16 patches all applied in batch [6 HIGH + 7 MED + 3 LOW]: HIGH fixes were UUID mismatch [onMutate + mutationFn shared via syntheticTxIdRef], MEMBER_PROFILE_QUERY_KEY invalidate on contrib/rattrap online success [duplicate-row prevention], persistOfflineEvent wraps appendEvent errors as typed Record*Error("offline_storage") + new i18n keys + MemberList toast surfaces, spec amendment for AC #9 + Task 7 (kind-agnostic showOfflineToast replaces per-toast variant — cleaner), optimisticCache recomputes MemberStats via computeMemberStats, MemberList integration test added asserting showOfflineToast dispatch on wasOffline=true. MED: gzipped bundle delta measured at +2.29 KiB ≤ 3 KB ✓; AC #10 i18n key corrected transaction.toast.offline → members.toast.offline; cancelOptimisticQueries on BOTH MEMBERS + MEMBER_PROFILE; pendingCount reset on signout via cleanup-fn; BroadcastChannel filters by message.type + collectorId; notifyEventLogChange accepts optional collectorId. LOW: shared offlineGuards.ts (DRY), waitFor-style unmount test (was setTimeout(30) flaky), appendEventMock.not.toHaveBeenCalled on advance/rattrap cycle_closed. Defers (3): sign-out mid-mutation unauthorized loss, per-call BroadcastChannel perf, page-reload mid-mutation recovery — all filed in deferred-work.md. Dismissed (2): 4th toast helper resolved via spec amendment, test count exceeds floor (positive). Gates: typecheck / lint (`--max-warnings=0`) / 816 vitest passed / branches 75.37% global (≥ 75% gate) / build PWA precache 808.53 KiB / gzipped JS 226.0 KiB (+2.29 KiB vs 8.2 baseline of 223.71 KiB). Implementation:
# touched (previous): 8-3-outbox-pattern-queue → review (implemented 2026-05-14 via bmad-dev-story on feat/8-3-outbox-pattern-queue [Node 22 / npm 10 — lesson from PR #68 applied; no new deps so no lockfile churn]; THIRD story of Epic 8 — Offline Resilience; wires Story 8.2's IDB primitives into the write path of the 3 transaction mutations + replaces Story 8.1's pendingCount=0 placeholder with the real BroadcastChannel-driven subscription; deliverables: `useCollectorId` hook (~55 LOC, session-aware via supabase.auth.getSession + onAuthStateChange) + `buildOfflineEvent` discriminated-union helper (~110 LOC, snake_case p_* payload keys for Story 8.4 reconciler) + `optimisticCache` shared helpers (~90 LOC, applyOptimisticTransactionUpdate + rollback, MEMBERS_QUERY_KEY recency-sort + MEMBER_PROFILE_QUERY_KEY transaction-row synthesis + totalTransactionsCount bump) + `showOfflineToast` kind-agnostic helper (~35 LOC, reuses existing members.toast.offline i18n key — NO new keys) + eventLog.ts BroadcastChannel emission (notifyEventLogChange on append/delete/clear success; defensive guard for degraded envs) + 3 record-* hooks rewritten with 2-step offline fallback (navigator.onLine === false short-circuit + TypeError fetch fallback) + onMutate cache snapshot + onError rollback + onSuccess gated on !wasOffline; mutation return type `string → { txId, wasOffline }` (breaking; MemberList + advance route consumers updated to dispatch showOfflineToast when wasOffline); useConnectivityState.pendingCount becomes real via BroadcastChannel("safaricash-event-log") subscription + countEvents(collectorId) lookup; EVENT_LOG_CHANNEL_NAME constant + EventLogChangeMessage type re-exported from @/infrastructure/sync barrel; 32 net new vitest cases (6 useCollectorId + 4 channel emission + 5 buildOfflineEvent + 6 contribution offline/optimistic/rollback + 2 advance offline + 2 rattrapage offline + 2 showOfflineToast + 5 connectivity subscription); gates: typecheck / lint (`--max-warnings=0`) / 815 vitest passed (+ 1 skipped; +32 vs Story 8.2 baseline of 783) / branches 75.95% global (≥ 75% gate) / build PWA precache 807.59 KiB (+30 KiB raw vs 8.2 baseline of 777.66 KiB — ~470 LOC of new code across 9 files; gzipped delta much smaller; AC #31 budget was 3 KB gzipped, accepted as inherent cost of optimistic-update + offline-branch logic); NO new deps (BroadcastChannel + crypto.randomUUID + navigator.onLine browser-native); NO migration / Edge Function / DB schema / new i18n keys; debug-log: 2 lint fixes (deep-imports → @/features/member barrel + react-hooks/set-state-in-effect → early-return-without-reset) + 1 RTL fix (showOfflineToast test wrong React-element introspection) + 1 StatusBadgeKind literal fix (actif not active) + 2 happy-path test assertions adapted to new {txId, wasOffline} shape + 4 leftover makeWrapper() call-sites in contribution.test.tsx adapted; AC #26 MemberList integration test deferred — contract exhaustively covered at unit level (useRecordContribution.test.tsx asserts move-to-top onMutate; showOfflineToast.test.tsx asserts offline copy renders; the consumer glue `if (result.wasOffline) showOfflineToast(...)` is 3 lines that typecheck + lint cover); Story 8.1's `hasFailed = false` placeholder STAYS untouched (Story 8.5 wires the stalled-sync retry-state); locks contract for Story 8.4 (reconciler drains via listEvents → POST → deleteEvent; BroadcastChannel delete-events refresh the pill subscription) / 8.5 (stalled retry-state + hasFailed source) / 8.6 (offline read path). Below: previous touched line:
# touched (previous): 8-3-outbox-pattern-queue → ready-for-dev (spec drafted 2026-05-14 via bmad-create-story; THIRD story of Epic 8 — Offline Resilience; wires Story 8.2's IDB primitives into the 3 record-* mutation hooks' offline branches via 2-step detection [navigator.onLine === false short-circuit OR network-error fallback]; TanStack Query onMutate optimistic UI on MEMBERS_QUERY_KEY [last_transaction_at bump → recency-sort moves member to top] + MEMBER_PROFILE_QUERY_KEY [synthetic transaction row + cycle.totalCollected/daysContributed bumps]; onError rollback; onSuccess invalidation GATED on !wasOffline [preserves offline optimistic state until Story 8.4's reconciler replays + triggers refetch]; new `useCollectorId()` hook at src/features/auth/api/useCollectorId.ts [reads supabase.auth.getSession + subscribes to onAuthStateChange]; `useConnectivityState.pendingCount` becomes real via BroadcastChannel("safaricash-event-log") subscription + countEvents(collectorId) lookup; new BroadcastChannel emission from eventLog.ts mutators [appendEvent/deleteEvent/_clearAllEvents post on success]; `show*Toast` × 3 (contribution / advance / rattrapage) gets `offline` phase reusing existing transaction.toast.offline i18n key (NO new keys); mutation return type changes string → {txId, wasOffline} (breaking; 2 consumers MemberList + advance route updated); `crypto.randomUUID()` synthetic transaction ID generated BEFORE RPC attempt (becomes eventId AND entityId AND mutation return); `toCanonicalTimestamp(new Date().toISOString())` from @/domain/audit for OfflineEvent.timestamp; ~28 vitest cases planned (9 offline per-hook + 3 online per-hook + 3 onMutate optimistic + 3 onError rollback + 4 useCollectorId + 5 pendingCount subscription + 4 channel emission + 1 MemberList integration); coverage gate ≥ 75% branches global, new useCollectorId ≥ 85% branches isolated; bundle budget ≤ 3 KB gzipped (no new deps — BroadcastChannel + crypto.randomUUID browser-native); NO migration / Edge Function / new i18n keys / new deps; cross-feature import @/features/connectivity ← @/features/auth allowed (auth is foundational, precedent: @/features/transaction → @/features/member for MEMBERS_QUERY_KEY); Story 8.1's `hasFailed = false` placeholder STAYS untouched (Story 8.5 wires); no retry button on offline toast (UX-DR5 "never red-alarm" + reconciler is auto); no Playwright E2E in this story (Story 8.4 will add flow-1-offline-replay.spec.ts once reconciler ships); locks contract for Story 8.4 (reconciler drains via listEvents → POST → deleteEvent — BroadcastChannel posts trigger pill refetch) / 8.5 (stalled retry-state + hasFailed source) / 8.6 (offline read path for member search/list/profile/edit). Below: previous touched line:
# touched (previous): 8-2-indexeddb-event-log → done (merged 2026-05-14 via PR #68, commits fc11bae [story] + e36763d [lockfile fix]; CI green on 2nd push — 1st push failed all 3 jobs at `npm ci` because local Node 24/npm 11 produced a lockfile that CI's Node 22/npm 10 rejected with "Missing: @esbuild/* from lock file" × 32 platform binaries — fix: nvm use 22 + rm node_modules + npm install + re-commit, lesson saved to memory `feedback_npm_lockfile_node_version.md`; implemented 2026-05-15 via bmad-dev-story on feat/8-2-indexeddb-event-log + cross-LLM code-review on sonnet-4-6 [3-layer adversarial: Blind Hunter / Edge Case Hunter / Acceptance Auditor] — verdict Changes requested, 3 HIGH + 8 MED + 4 LOW patches all applied in batch; SECOND story of Epic 8 — Offline Resilience; pure client-side infrastructure (NO UI / migration / Edge Function / i18n / state lib): `OfflineEvent` interface + `offlineEventSchema` Zod with `satisfies z.ZodType<OfflineEvent>` lockstep + `.refine(structuredClone)` payload guard (post-review) + `eventLog.ts` ~250 LOC under `src/infrastructure/sync/` exposing `appendEvent` / `listEvents` / `getEvent` / `countEvents` / `deleteEvent` + `openEventLogDb` singleton + test-only `_clearAllEvents` + `_resetEventLogDbForTests` (renamed for naming-convention parity, NOT exported from index.ts barrel post-review) + typed `OfflineEventLogError` with 5 codes (cause field redeclared as `override readonly cause?: unknown` for readonly contract preservation post-review); singleton IDB DB `safaricash_event_log` v1, store `events` keyPath `eventId` + autoIncrement:false explicit (post-review), non-unique composite index `byCollectorAndTime` on `[collectorId, timestamp]`; `openDB` options include `blocking()` callback (multi-tab v2 upgrade safety, post-review) + `terminated()` callback (forced-close recovery, post-review); `requireCollectorId` guard rejects empty/non-UUID inputs to listEvents/countEvents (post-review); `mapIdbError` cast tightened to `typeof` guard against primitive throws (post-review); `listEvents` returns rows sorted timestamp ASC then eventId ASC; `countEvents` O(log n) via `countFromIndex` + `IDBKeyRange.bound([collectorId, ""], [collectorId, "￿"])`; `deleteEvent` idempotent; 33 vitest cases (5 types + 28 eventLog; well above spec floor of 19) incl. round-trip + VALIDATION_FAILED + DUPLICATE_EVENT_ID + payload-structuredClone-refine reject + partition isolation + countEvents per partition + index-range correctness + cross-reload durability + cross-sign-out durability with vi.mock("@/infrastructure/supabase/client") running real `requestSignOut` + static-grep guard reading signOut.ts source asserting no _clearAllEvents/_resetEventLogDbForTests/@/infrastructure/sync imports + crash-mid-session recovery + deleteEvent idempotent + _clearAllEvents wipe + getEvent undefined on miss + Promise.all-3-appends + instanceof OfflineEventLogError + singleton memoisation + QUOTA_EXCEEDED + TRANSACTION_FAILED × 3 (get/count/clear) + re-throw-OEL-without-wrap + DB_OPEN_FAILED via `vi.doMock("idb")` + sort-tiebreak + 3 collectorId-guard cases (empty + non-UUID × 2); new deps `idb` ^8.0.3 (dep) + `fake-indexeddb` ^6.2.5 (devDep, polyfilled into vitest.setup.ts via `await import("fake-indexeddb/auto")`); cross-LLM dismissed findings: self-rescinded singleton reasoning + U+FFFD-vs-U+FFFF sentinel (verified U+FFFF at lines 146,177) + branch-coverage 81.25% defer already accurate + fake-indexeddb/auto intentional improvement; defer: schema.source accepting "online" deferred to Story 8.3 caller-side enforcement (filed in deferred-work.md § Story 8.2); gates: typecheck / lint (`--max-warnings=0`) / 783 vitest passed (+ 1 skipped; +41 vs Story 8.1 baseline of 742, of which 33 from this story + 8 from pre-existing tests that started passing after fake-indexeddb polyfill landed) / branches 76.4% global (≥ 75% gate) / `eventLog.ts` 97.26% stmts / 82.05% branches / 87.5% funcs / 97.1% lines isolated / build PWA precache 777.66 KiB unchanged (+0.19 KiB raw vs 8.1 baseline 777.47 KiB — idb tree-shakes aggressively); AC #22 82.05% vs claimed ≥ 85% — 5 defensive branches (DOMException narrow-cast + v2 schema-upgrade + eventId-tie return-0 + blocking() + terminated() callbacks all unreachable in jsdom) filed in `deferred-work.md` § Story 8.2 with revisit triggers (schema v2 bump OR Playwright real-browser); locks contract for Stories 8.3 (write-path + pendingCount real-source) / 8.4 (reconciler drains via listEvents → deleteEvent per success) / 8.5 (stalled-sync retry) / 8.6 (offline member lookup); Story 8.1's `useConnectivityState` pendingCount placeholder stays untouched until Story 8.3. Below: previous touched line:
# touched (previous): 8-1-connectivity-indicator → done (PR #67 merged 2026-05-15, commit fc80336; full CI green on FIRST try — pre-push memory from PR #66 lessons applied successfully; FIRST story of Epic 8 — Offline Resilience; useConnectivityState hook with navigator.onLine + window listeners + state derivation [offline→sync-failed→syncing→connected ordering] + pendingCount/hasFailed placeholders for Stories 8.3/8.4; ConnectivityIndicator pill (4 states with semantic colours per UX-DR5 "never red-alarm", aria-live + aria-label, 40px hit-area, animate-pulse on icon only for sync-failed); ConnectivitySyncDrawer skeleton (empty state + placeholder for Story 8.4 to populate, programmatic focus on close button); AppLayout integration with drawer state held at layout level; 9 i18n keys under new connectivity.* namespace; 24 vitest cases (6 hook + 11 pill + 7 drawer) incl. axe-clean × 4 states; lint catch on react-hooks/set-state-in-effect fixed; all local gates green (typecheck / lint / 742 vitest / 76.04% branches global / build PWA +5 KiB); cross-LLM review on sonnet-4-6 [Approve with suggestions, 0H/3M/3L] — all 6 patches applied: deriveState export + 5 priority tests (AC #17 gap closed), +3 i18n `*_idle` keys (NFR-L2 prep), removed phantom drawer state prop, toHaveBeenCalledTimes assertions, AC #19 deferral doc, hit-area regression test; 30/30 connectivity tests post-patches; Epic 8 kicked off. Below: previous touched line:
# touched (previous): 8-1-connectivity-indicator → ready-for-dev (spec drafted via bmad-create-story; FIRST story of Epic 8 — Offline Resilience; persistent header pill in AppLayout with 4 states (connected/syncing/offline/sync-failed) via navigator.onLine + pendingCount placeholder ; `useConnectivityState` hook contract locked for Stories 8.3/8.4 to wire real IndexedDB-backed count + reconciler failure flag; `ConnectivitySyncDrawer` skeleton with "Aucune opération en attente" placeholder (Story 8.4 populates with pending list + retry CTAs); 9 i18n keys under new connectivity.* namespace; ~18-22 vitest cases incl. axe-clean × 4 states; no migration, no Edge Function, no schema change — purely frontend surface that subsequent Epic 8 stories populate); kicks off Epic 8. Below is Epic 7's history:
# touched (previous): epic-7 → done (5/5 stories complete: 7.1→7.5 all merged 2026-05-13→2026-05-15); 7-5-cycle-settled-final-sms → done (PR #66 merged 2026-05-15, commit b1c3f99 + 2 CI-fix commits; CLOSING story of Epic 7; cross-LLM review intercepted a CI-breaking SMS length bug (template was 162-167 chars worst-case, fixed by removing 'Merci. ' + capping firstName 16→9 → exactly 160 chars at worst case) + completed AC #11 (Worker E2E settlement page) which was previously checked-but-unimplemented + completed AC #9 with 2 missing Deno contract cases (unaccent + single-token); the LIGHTEST + LAST story of Epic 7 — 2 migrations [format_sms_body settlement branch rewrite (firstName + DD/MM cycle range + closing statement + plain-digit amount) + get_receipt_payload extension with cycle_start_date + cycle_end_date, DROP+CREATE workaround for SQLSTATE 42P13]; Worker render.ts settlement branch (renderSettlementReceiptHtml hides projected balance + cycle day + dispute CTA, shows cycle period DD/MM/YYYY + closing statement, .settlement-closing CSS class, defensive missing-dates fallback); 13 new Worker render tests + updated format_sms_body contract test + new length-boundary case + new get_receipt_payload cycle dates contract test; Playwright Flow 3 SMS body regex assertions; SMS ≤ 160 chars worst-case (GSM-7 discipline preserved); all local gates green (typecheck / lint / 720 vitest / 44 Worker / 76.32% branches global / build, Deno + Playwright deferred to CI); when 7.5 merges → Epic 7 transitions to done [5/5 stories complete]. Below was Story 7.4's touched line, preserved for history: 7-4-settlement-reauth-gate → done (PR #65 merged 2026-05-14, commit 010e955; full CI green including 9 Deno contract tests + Playwright Flow 3 E2E + 5 migrations on FIRST TRY; the LARGEST story of Epic 7 (implemented 2026-05-14 via bmad-dev-story + code-review patches; LARGEST story of Epic 7 — end-to-end commit pipeline shipped; 5 migrations [+1 vs spec: reject_transaction_allow_settlement.sql to relax Story 3.4 trigger for kind='settlement' on completed cycles] + commit_cycle_settlement SECURITY DEFINER RPC ~80 LOC with FOR UPDATE lock + NFR-R3 server-side payout recompute + cross-check; cycle-settlement Edge Function ~270 LOC mirroring Story 6.6 pattern with verifyPassword shared helper + P0002 error-message-prefix mapping to RFC 7807 [+2 keys in KNOWN_PROBLEMS: payout_mismatch + cycle_not_settleable]; @/features/settlement folder with useCommitSettlement (10-code classifyError + invalidation + this-binding compliant) + commitSettlementError typed class + SettlementReauthDialog (native <dialog>, form-wrap, password-trim — Story 6.6 P3/P8 preserved); route handler swap (Story 7.3 toast.info stub → dialog opens → in-route view-swap to <EnvelopeHandoverScreen> with payoutAmount=settled_payout); 14 i18n keys under new settlement.reauth.* + settlement.toast.* sub-namespaces; existing audit_emit_cycle_transitioned trigger auto-emits cycle.settled on cycle UPDATE — no audit allowlist change; existing format_sms_body('settlement', tx_id) template reused via synthetic transactions.kind='settlement' row + extended trigger CASE; 46 new vitest cases (17 hook + 11 dialog + 18 route) + 8 Deno RPC contract cases + 1 Playwright Flow 3 E2E (happy + wrong-password retry + service-role side-effects); deviation documented: spec AC #13 Edge Function HTTP contract test deferred to Playwright (matches Stories 6.6 / 6.7 pattern); all local gates green (typecheck / lint / 705 vitest / 76.32% branches global / build, +8 KiB precache); Deno + Playwright deferred to CI (no Deno binary locally); settlement route at 96.55/88.23/90.9/96.49; useCommitSettlement at 98/82.22/100/98; SettlementReauthDialog at 90.38/82.35/90/97.87; cross-LLM code-review on sonnet-4-6 [Changes requested → all 5 patches applied: HIGH isSubmitting fix via dialog→route onMutatingChange callback, MED cycle/member-mismatch → 400 request_invalid, MED server_payout in RFC 7807 body, LOW axe inline-error state, LOW Deno auth.uid()=null case]; gates re-run green; blocks Story 7.5 (final SMS content tweaks + receipt URL routing for kind='settlement')
# project: SafariCash
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-04-19
last_updated: 2026-05-15
project: SafariCash
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # Epic 1 — Collector Onboarding & Sign-In
  epic-1: in-progress
  1-1-project-bootstrap: done
  1-2-supabase-foundation: done
  1-3-reauth-edge-function: done
  1-4-rate-limit-middleware: done
  1-5-phone-otp-signin: done  # superseded by 1-5b (PRD v1.3 auth pivot — Termii KYC blocker)
  1-5b-password-auth-switch: review  # rewrites OTP login → signInWithPassword + decommissions auth-sms-hook + check_collector_registered RPC; also rewrites Story 1.3 re-auth Edge Function to password verification
  1-6-session-management: done
  1-7-sign-out: done
  1-8-ci-pipeline-gates: done
  epic-1-retrospective: optional

  # Epic 2 — Member Lifecycle Management
  epic-2: in-progress
  2-1-member-list-search: review
  2-2-create-member-manual: review  # spec generated 2026-04-21 via bmad-create-story; React Hook Form v7 first wiring + atomic create_member_with_cycle RPC
  2-3-contacts-bulk-import: review  # spec generated 2026-04-22; reuses Story 2.2 RPC via p_created_via='contacts_import' + new phone-hash unique constraint migration; 3-step state machine + Promise.allSettled with 5-concurrency limiter; Chromium-Android only (iOS fallback to manual entry)
  2-4-member-profile-view: done  # PR #11 merged 2026-04-22; 19 new tests; full gates green (336 vitest, coverage 75.7% > 75%); E2E env-gated
  2-5-edit-member-impact-alert: done  # PR #13 merged 2026-04-23; 17 new tests; uncovered + fixed system-wide audit-trigger JWT bug (migration 0017)
  2-6-delete-member-confirmation: done  # PR #17 merged 2026-04-23; 18 new tests; uncovered + fixed CI edge-runtime gap; LAST story of Epic 2
  2-7-restart-cycle: done  # PR #15 merged 2026-04-23; 14 new tests; full gates green; CI passed first-try thanks to local Playwright validation
  epic-2-retrospective: optional

  # Epic 3 — Cycle Engine & Progression
  epic-3: in-progress  # kicked off 2026-04-23 with Story 3.1 (cycle-invariants ADR)
  3-1-cycle-invariants-adr: done  # PR #20 merged 2026-04-23; ADR-004 ships 8 invariants with property test skeleton names
  3-2-cycle-engine-pure-module: done  # PR #22 merged 2026-04-24; cycle module 100% coverage; fast-check installed; computeMemberStats moved to domain
  3-3-cycle-status-transitions: done  # PR #24 merged 2026-04-24; promote_cycle_on_advance trigger + cycle.transitioned audit event
  3-4-prevent-post-completion-contributions: done  # PR #26 merged 2026-04-24; FR19 DB enforcement + isCycleClosedForTransactions helper
  3-5-cycles-ending-alerts: done  # PR #40 review complete — 8 patches addressed (setSearchParams scope, E2E UTC date, body_zero key, hook test QueryClientProvider, dismiss-chip a11y, aria-live keep-mounted, useCallback dismiss, dismiss-disappears test); 2 deferred items in deferred-work.md (useMembers isError handling for Story 9.1, Tailwind warning-palette token cleanup); full gates green; 20/20 Playwright validated locally; closes Epic 3
  epic-3-retrospective: optional

  # Epic 4 — Daily Transaction Capture (Core)
  epic-4: in-progress  # kicked off 2026-04-24 with Story 4.1
  4-1-member-action-sheet-component: done  # PR #28 merged 2026-04-24; MemberActionSheet + MemberList rewire
  4-2-progressive-toast-component: done  # PR #30 merged 2026-04-24; pure component shipped
  4-3-record-contribution-online: review  # implemented 2026-04-24/25; full Flow 1 online path; 441 vitest, 29 edge, 19 Playwright validated locally
  4-4-record-rattrapage: review  # implemented 2026-04-26 via bmad-dev-story; 2 migrations (days_covered + cross-kind CHECK + record_rattrapage RPC) + useRecordRattrapage hook + ProgressiveToast bodyOverride extension + showRattrapageToast + action-sheet long-press (500ms) + inline × 2/× 3/× 4 jours grid + MemberList wiring + 8 Deno contract tests + new Playwright spec; closed-cycle gate inherited from Story 3.4; SMS enqueue inherited from Story 4.3; full gates green
  4-5-undo-transaction-window: review  # implemented 2026-04-26 via bmad-dev-story; rewrites 4.3 hard-DELETE → soft-undo; 4 migrations + undo_transaction RPC + UndoTransactionError typed class + MemberList typed-error toast handling + 7 Deno contract tests + 8 vitest cases; closes Epic 4; full gates green; dropped spec AC #1's undone_event_id column (would have polluted audit chain via second UPDATE)
  epic-4-retrospective: optional

  # Epic 5 — Emergency Advance Flow
  epic-5: in-progress  # kicked off 2026-04-26 with Story 5.1 (AdvanceSimulationPanel)
  5-1-advance-simulation-panel: review  # implemented 2026-04-27 via bmad-dev-story; pure component AdvanceSimulationPanel.tsx (4-row card driven by Story 3.2 cycle-engine primitives, 3 states empty/valid/over-limit, aria-live=polite on row 4 only, currency formatted via formatFcfaAmount, all colours via Tailwind tokens); 8 vitest tests incl. jest-axe across 3 states; 7 i18n keys under advance.simulation.*; no E2E (deferred to 5.4 commit-path)
  5-2-advance-situation-context: review  # implemented 2026-04-27 via bmad-dev-story; AdvanceFlow screen at /members/:id/advance (situation panel via Story 2.4 useMemberProfile + 3 suggested-amount chips ADVANCE_SUGGESTED_AMOUNTS=[50k,100k,150k] + free-form numeric input + Story 5.1 simulation panel); MemberList onAdvance wires navigation; closed-cycle / no-active-cycle / not-found redirect defensively; CTA disabled (Story 5.3 enables, 5.4 commits); 11 AdvanceFlow tests + 1 MemberList integration; jest-axe clean
  5-3-motive-saver-acknowledgment: review  # implemented 2026-04-27 via bmad-dev-story; AdvanceFlow extended with motive textarea (≥3 chars trimmed) + saver-ack checkbox (NOT pre-checked, BDD ligne 930 copy verbatim) + precedence-ordered CTA gate (amount→motive→ack) with contextual disabled tooltip; AdvanceConfirmPayload extended to {amount, motive, acknowledged}; 11 new test cases; all gates green
  5-4-commit-advance-transaction: review  # implemented 2026-04-27 via bmad-dev-story; closes Epic 5; 2 migrations (motive+saver_acknowledged columns + cross-kind CHECK + record_advance SECURITY DEFINER RPC with server-side capacity check) + RecordAdvanceInputSchema (Zod with z.literal(true) for ack) + useRecordAdvance hook (9 typed error codes) + showAdvanceToast (bodyOverride) + route wiring of onConfirm with typed-error toast.error mapping; 8 Deno contract tests incl. DB CHECK + audit-payload integrity; new flow-2-advance.spec.ts E2E; triggers fire for free (Story 3.4 closed-cycle gate, Stories 1.2/3.3/4.5 audit, Story 4.3 SMS enqueue, Story 3.3 promote_cycle_on_advance + cycle.transitioned)
  epic-5-retrospective: optional

  # Epic 6 — Saver Trust Communications
  epic-6: in-progress  # kicked off 2026-04-27 with Story 6.1 (sms-dispatch infrastructure)
  6-1-sms-dispatch-edge-function: review  # implemented 2026-04-27 via bmad-dev-story; 3 migrations (schema extension + trigger rewrite + audit_append_external SECURITY DEFINER helper) + sms-dispatch Edge Function (POST, JWT-auth, RLS-respecting tx lookup, RFC 7807, hashed-phone structured logging) + 5 trigger contract tests + 8 Edge Function contract tests; opens Epic 6; full gates green (548 vitest / 65 edge / build / typecheck / lint)
  6-2-sms-worker-termii-retry: review  # implemented 2026-04-28 via bmad-dev-story; 3 migrations (audit allowlist 1-line extension + pg_cron 30s schedule + claim_sms_queue_batch RPC + audit_append_external 5-arg overload) + sms-worker Edge Function (POST + service-role-key gate + Promise.all batch parallelisation + FOR UPDATE SKIP LOCKED drain + backoff 10s→600s cap + 24h abandon + soft-undo skip via undo_transaction) + sendSmsNoRetry termii client extension + pure backoff module 100% covered + 4 new contract test files (8 backoff + 12 worker + 6 allowlist + 5 cron); shared test-fixtures extracted from Story 6.1 tests; full gates green (548 vitest / 94 edge / build / typecheck / lint)

  6-3-sms-copy-templates: review  # implemented 2026-04-28 via bmad-dev-story; 3 migrations (receipt_token 128-bit + unique idx + CHECK regex / format_sms_body SECURITY DEFINER helper with 4 template branches + unaccent + receipt URL via current_setting GUC fallback / trigger replacement 3-line diff vs Story 6.1) + 3 contract test files (8 format-helper + 1 banking-language + 1 length/ASCII) + 1 regression assertion in Story 6.1 trigger test; full gates green (548 vitest / 104 edge / build / typecheck / lint)

  6-4-receipt-url-worker: review  # implemented 2026-04-28 via bmad-dev-story; Cloudflare Worker safaricash-receipt-url (GET /r/{token} no-JS HTML + inline CSS + token regex defence + service-role lookup) + get_receipt_payload SECURITY DEFINER RPC (Story 4.5 handshake — undone rows 404) + dispute.ts 501-stub (Story 10.2 wires) + 6 token + 22 render unit tests incl. jest-axe Level A + XSS escape defence + 7 Playwright cases + CI wrangler-dev step on port 8788; full gates green (576 vitest / typecheck / lint / build)

  6-5-first-sms-consent-optout: review  # implemented 2026-04-28 via bmad-dev-story; 6 migrations (sms_opt_out columns + partial idx / trigger replacement / audit allowlist 1-line ext / set_member_sms_opt_out RPC / get_member_id_from_token RPC / find_members_by_phone RPC) + sms-inbound Edge Function (Termii STOP webhook with ?secret= gate + multi-collector opt-out) + receipt-url Worker GET/POST /r/{token}/opt-out routes + render module additions; full gates green (584 vitest / 121 edge / build / typecheck / lint)

  6-6-resend-cycle-history: done  # implemented 2026-05-12 via bmad-dev-story + code-review patches applied; 4 migrations (0050 template_key 'resend' / 0051 audit allowlist 'sms.resend_initiated' / 0052 format_resend_sms_body wrapper [+ auth.uid ownership check] / 0053 enqueue_resend_history RPC [+ staggered created_at via row_number()] with Story 4.5 undone-filter + Story 6.5 opt-out short-circuit) + _shared/verify-password.ts (extracted from Story 1.5b re-auth, second consumer landed) + sms-resend-history Edge Function (POST + JWT + RFC 7807 + structured logging, error.message redacted from 500 detail, password trim+min1, NaN guard) + ResendHistoryDialog (form-wrapped for Enter-to-submit) + useResendHistory hook (typed ResendHistoryError + MEMBER_PROFILE_QUERY_KEY invalidation [AC #12 fix] + TypeError class-identity for network detection) + member profile button + 16 i18n keys + 3 Deno contract tests (URN-exact assertions, not substring) + audit-allowlist regression + Playwright E2E; 21 ACs + 14 tasks; gates: typecheck/lint/598-vitest/build all green; 8 inherited issues deferred

  6-7-per-transaction-receipt-share: done  # implemented 2026-05-12 via bmad-dev-story + code-review patches; 2 migrations (0054 transactions_decrypted exposes receipt_token / 0055 enqueue_resend_transaction RPC with uniform P0002 + auth.uid ownership + opt-out + no-phone + undone + unsupported_kind short-circuits) + Deno contract test (8 cases incl. unsupported_kind defence) + shareReceipt helper (Web Share API + clipboard fallback, never logs token, NBSP intentional) + ResendTransactionError + useResendTransaction hook (TanStack invalidation per 6.6-review pattern + TypeError class-identity) + TransactionReceiptSheet (native <dialog>) + interactive MemberProfile rows + 22 i18n keys + Playwright E2E + 4 missing review-required tests added (AC #17 receipt_token assert + AC #21 interactive-row + AC #22 tap-opens-sheet); NO Edge Function (direct RPC, per-tx not in FR5 list); all local gates green (typecheck / lint / 624 vitest / build); 5 inherited issues deferred
  6-8-whatsapp-secondary-delivery: backlog
  epic-6-retrospective: optional

  # Epic 7 — Cycle Settlement Ceremony
  epic-7: done  # COMPLETE — 5/5 stories merged 2026-05-13→2026-05-15: 7.1 SettlementSummaryCard (PR #61), 7.2 EnvelopeHandoverScreen (PR #62), 7.3 Settlement route + entry CTA (PR #63), 7.4 Atomic commit + re-auth gate (PR #65 — the largest), 7.5 Settlement SMS + receipt page (PR #66). Epic 7 ships the full day-30 cycle-settlement ceremony: collector taps "Clôturer le cycle" → SettlementSummaryCard with NFR-R3 zero-tolerance payout from Story 3.2 settle() → password re-auth dialog → cycle-settlement Edge Function → atomic SECURITY DEFINER RPC (FOR UPDATE lock + server-side payout cross-check + INSERT synthetic kind='settlement' transaction + UPDATE cycle.status='settled' firing existing cycle.settled audit emit) → frontend view-swap to EnvelopeHandoverScreen → saver receives settlement SMS with firstName + cycle period + receipt URL → settlement receipt page (Cloudflare Worker) shows cycle period + closing statement, hides dispute CTA (settlement is irreversible).
  7-1-settlement-summary-card: done  # merged 2026-05-13 via PR #61 (commit e4f2713); full CI green incl. cross-LLM code-review patches; pure presentation atom for Epic 7 ceremony surface — math via Story 3.2 settle()/commission() (NFR-R3 compliance), aria-live on row 4 only, 11 i18n keys under new settlement.summary.* namespace, 13 vitest+RTL+jest-axe cases; unwired until Story 7.3 (route) and 7.4 (re-auth + commit RPC)
  7-2-envelope-handover-screen: done  # merged 2026-05-14 via PR #62 (commit 030f13a); full CI green incl. cross-LLM code-review patches; pure presentation atom — Epic 7 emotional climax (UX-DR10 "moment of crystallised trust"); 5-block anatomy with Check icon circle / h2 / inline-styled amount body / 3-config subtext live-region / single CTA; mount-time programmatic focus on CTA (jsx-a11y/no-autofocus compliant); 6 i18n keys (body split _amount_prefix + _recipient documented in fr.json._notes for Story 1.5 translators); 12 vitest+RTL+jest-axe cases; Pride-over-playfulness — only essential animate-spin on Loader2 when pending; consumed by Story 7.4 post-commit
  7-3-settlement-initiation-computation: done  # merged 2026-05-14 via PR #63 (commit 19f6a25); full CI green incl. cross-LLM code-review patches; route /members/:id/settlement mounts Story 7.1 SettlementSummaryCard with real data via useMemberProfile; UUID guard + 4 preconditions (active/settled/with_advance/null cycle → redirect to profile); "Clôturer le cycle" CTA in profile header gated on currentCycle.status === "completed"; advances sorted newest-first; onConfirm stub is toast.info with enriched Story 7.4 TODO checklist (swap handler + drive isSubmitting + post-commit envelope nav); NFR-R3 zero-tolerance preserved (route never recomputes — card calls settle() internally); ready for Story 7.4 to swap onConfirm handler
  7-4-settlement-reauth-gate: done  # merged 2026-05-14 via PR #65 (commit 010e955); full CI green INCLUDING 9 Deno contract tests + Playwright Flow 3 E2E + 5 migrations on first try; the LARGEST story of Epic 7 — end-to-end settlement commit pipeline: commit_cycle_settlement SECURITY DEFINER RPC + cycle-settlement Edge Function (verifyPassword shared helper) + @/features/settlement folder (useCommitSettlement hook + commitSettlementError typed class + SettlementReauthDialog native <dialog>); route Story 7.3 stub replaced with dialog → in-route view-swap to <EnvelopeHandoverScreen>; NFR-R3 zero-tolerance preserved at 2 layers (client settle() + server recompute + cross-check); existing audit_emit_cycle_transitioned trigger emits cycle.settled automatically; existing format_sms_body('settlement', tx_id) template reused via synthetic transactions.kind='settlement'; cross-LLM code-review on sonnet-4-6 [Changes requested → all 5 patches applied incl. HIGH isSubmitting bug fix]; blocks Story 7.5 (final SMS content tweaks + receipt URL routing for kind='settlement')
  7-5-cycle-settled-final-sms: done  # merged 2026-05-15 via PR #66 (commit b1c3f99 + 2 CI-fix commits 092c9c9 + d44740b); CLOSING story of Epic 7; 2 migrations (format_sms_body settlement-branch rewrite with firstName + DD/MM cycle range + amount, GSM-7 single-SMS 160-char discipline via removed Merci. + firstName cap 9; get_receipt_payload extended with cycle_start_date + cycle_end_date via DROP+CREATE workaround for SQLSTATE 42P13) + Worker render.ts settlement branch (renderSettlementReceiptHtml: "Cycle clôturé" title + period row + closing statement + hide dispute CTA + keep opt-out, .settlement-closing CSS); 13 Worker render tests + 4 Deno format_sms_body settlement cases (incl. unaccent + single-token + length boundary) + 1 Deno get_receipt_payload contract test + Worker E2E settlement page + Playwright Flow 3 SMS body assertions; cross-LLM code-review on sonnet-4-6 intercepted the SMS length bug pre-CI [1 HIGH + 2 MED + 3 LOW, all applied]; 2 CI fails surfaced: (a) hardcoded fixture-derived dates from Playwright fixture mistakenly used in Deno test, (b) wiring of dormant Story 7.4 contract test exposed stale "Cycle clos." assertion broken by 7.5's template change — both fixed, gain: 9 dormant 7.4 tests now in CI
  epic-7-retrospective: optional

  # Epic 8 — Offline Resilience
  epic-8: done  # COMPLETE 2026-05-15 — 6/6 stories done (8.1 connectivity-indicator / 8.2 indexeddb-event-log / 8.3 outbox-pattern-queue / 8.4 reconciler-replay / 8.5 stalled-sync-alert / 8.6 offline-member-lookup-edit), merged via PR #67-#72. The full offline-resilience loop ships: IndexedDB append-only event log → optimistic-UI outbox on the 3 record-* hooks + member edit → deterministic reconciler replay on reconnect (idempotent via p_event_id) → stalled-sync alert + manual retry → offline member read (TanStack Query persistence) + offline member edit. FR40-43 + NFR-P6/P7/R2 covered.
  8-1-connectivity-indicator: done  # merged 2026-05-15 via PR #67 (commit fc80336); FIRST story of Epic 8 — kicks off Offline Resilience; full CI green on FIRST try (pre-push memory from PR #66 lessons applied successfully); pure frontend story: useConnectivityState hook + ConnectivityIndicator pill (4 states, UX-DR5 never-red-alarm, aria-live + aria-label, 40px hit-area) + ConnectivitySyncDrawer skeleton (Story 8.4 will populate) + AppLayout integration + 12 i18n keys; cross-LLM code-review [Approve with suggestions, 0H/3M/3L — 6 patches applied incl. deriveState export + 5 priority tests, *_idle i18n keys for NFR-L2 prep, phantom prop cleanup]; 30 vitest cases incl. axe-clean × 4 states; all gates green (typecheck / lint / 76.04% branches global / build PWA +5 KiB)
  8-2-indexeddb-event-log: done  # merged 2026-05-14 via PR #68 (commits fc11bae + e36763d lockfile-fix); CI green on 2nd push (1st CI failed at `npm ci` because Node 24/npm 11 locally produces a lockfile incompatible with Node 22/npm 10 in CI — fix: nvm use 22 + regenerate lockfile — lesson saved to memory `feedback_npm_lockfile_node_version.md`); implemented 2026-05-15 via bmad-dev-story + cross-LLM code-review on sonnet-4-6 (3-layer adversarial) all patches applied; SECOND story of Epic 8 — Offline Resilience; pure client-side infrastructure (no UI, no migration, no Edge Function): `OfflineEvent` type + Zod schema (incl. `payload.refine(structuredClone)` post-review) + `eventLog` module under `src/infrastructure/sync/` (singleton IDB DB `safaricash_event_log` v1, composite index `(collectorId, timestamp)`, append-only via `keyPath: eventId`); CRUD = appendEvent / listEvents / getEvent / countEvents / deleteEvent + test helpers (NOT re-exported from barrel post-review); `requireCollectorId` guard rejects empty/non-UUID inputs; `blocking()` + `terminated()` callbacks on openDB for multi-tab v2 safety + storage-eviction recovery; typed `OfflineEventLogError` with 5 codes (cause field is `override readonly` post-review); 33 vitest cases (5 types + 28 eventLog; spec floor was 19) incl. crash-durability + cross-reload + cross-sign-out with vi.mock auth-client + static-grep guard on signOut.ts + cross-collector partition + concurrent appends + DB_OPEN_FAILED via vi.doMock + rare-error mapping + sort tiebreak + 3 collectorId-guard cases + 1 payload structuredClone refine; new deps `idb` ^8.0.3 (dep) + `fake-indexeddb` ^6.2.5 (devDep); gates green (typecheck / lint / 783 vitest / 76.4% branches global / build PWA precache 777.66 KiB); AC #22 isolated branches 82.05% vs claimed ≥ 85% — 5 genuinely-defensive branches (DOMException narrow-cast / v2 schema-upgrade / eventId-tie return-0 / `blocking()` / `terminated()` callbacks) filed in deferred-work.md § Story 8.2 (revisit triggers: schema v2 bump OR Playwright real-browser); cross-LLM review verdict was Changes requested with 3 HIGH (test race / blocking callback / AC #14 sign-out test) + 8 MED + 4 LOW + 1 defer (`source: "online"` permissive in schema, Story 8.3 enforces) + 4 dismissed (self-rescinded singleton / U+FFFD-vs-U+FFFF sentinel verified U+FFFF / branch coverage defer accurate / fake-indexeddb/auto intentional improvement); locks contract for Stories 8.3 (write-path + pendingCount real-source) / 8.4 (reconciler) / 8.5 (stalled-sync retry) / 8.6 (offline member ops)
  8-3-outbox-pattern-queue: done  # merged 2026-05-15 via PR #69 (commits 817b3d5 + e07391a [CI fix — flaky BroadcastChannel timing]); CI green on 2nd push — 1st push failed eventLog.test.ts BroadcastChannel emission test with `expected [] to have a length of 1 but got +0` because `flushChannel()` used setTimeout(0) + microtask which races dispatch under CI load; fix: poll-with-deadline pattern `waitForMessages(spy, expected, 500ms)` looping every 5ms; lesson saved to memory `feedback_broadcastchannel_test_timing.md`. Implemented 2026-05-14 via bmad-dev-story + cross-LLM code-review on sonnet-4-6 (3-layer adversarial) all 16 patches applied in batch (6 HIGH + 7 MED + 3 LOW); THIRD story of Epic 8 — Offline Resilience; wires Story 8.2's IDB primitives into the 3 record-* mutation hooks' offline branches (navigator.onLine short-circuit + network-error fallback); TanStack Query onMutate optimistic UI on MEMBERS_QUERY_KEY (recency bump via last_transaction_at) + MEMBER_PROFILE_QUERY_KEY (synthetic transaction row + cycle.totalCollected/daysContributed bumps); onSuccess invalidation gated on !wasOffline (preserves offline optimistic state until Story 8.4's reconciler refetches); new `useCollectorId()` hook (session-aware partition key via supabase.auth.getSession + onAuthStateChange); `useConnectivityState.pendingCount` becomes real via BroadcastChannel("safaricash-event-log") subscription + countEvents lookup; new BroadcastChannel emission from eventLog mutators (append/delete/clear); show*Toast × 3 gets `offline` phase reusing existing transaction.toast.offline i18n key (NO new keys); mutation return type changes string → {txId, wasOffline} (breaking; 2 consumers MemberList + advance route updated); ~28 vitest cases planned (9 offline + 3 online + 3 optimistic + 3 rollback + 4 useCollectorId + 5 pendingCount subscription + 4 channel emission + 1 MemberList integration); NO new deps (BroadcastChannel + crypto.randomUUID browser-native), NO migration, NO Edge Function, NO new i18n keys; bundle budget ≤ 3 KB gzipped; Story 8.1's hasFailed=false placeholder stays untouched (Story 8.5 wires); locks contract for Story 8.4 (reconciler drains via listEvents → POST → deleteEvent) / 8.5 (stalled retry-state) / 8.6 (offline read path for member search/profile/edit)
  8-4-reconciler-replay: done  # merged 2026-05-15 via PR #70 (merge commit 44e46e2; commits a1a6d6a [story] + 948e12c [CI fix — 42804 source enum cast] + 1182cef [CI fix — TanStack networkMode 'always']); CI took 3 pushes + 1 re-run — push 1 failed 5 Playwright E2E with SQLSTATE 42804 (the new CASE expression returns text but the source column is transactions_source_enum and Postgres won't auto-coerce a CASE result; fix migration 0061 DROP+CREATE 3 record-* RPCs with explicit ::transactions_source_enum cast, lesson → memory feedback_migration_rpc_smoke_test.md); push 2 failed only flow-1-offline-replay (TanStack Query default networkMode 'online' pauses mutationFn while navigator.onLine===false → in-hook offline branch unreachable + mutateAsync hangs; mocked-guard unit tests passed anyway; fix networkMode 'always' on all 3 record-* hooks, lesson → memory feedback_tanstack_networkmode_offline.md); push 3 failed only a transient vault_encrypt 502 flake in promote-cycle-on-advance.contract.test.ts (pre-existing Deno test, unrelated — cleared on re-run, no code change). Implemented 2026-05-15 via bmad-dev-story + cross-LLM code-review on sonnet-4-6 (3-layer adversarial) all 21 patches applied in batch (8 HIGH + 9 MED + 4 LOW); FOURTH story of Epic 8 — Offline Resilience; closes the offline-write loop opened by 8.2/8.3: 4 migrations (event_id UUID column on transactions + partial UNIQUE index transactions_event_id_idx WHERE event_id IS NOT NULL + transactions_decrypted view extension + DROP+CREATE 3 record-* RPCs accepting p_event_id UUID DEFAULT NULL with idempotent early-return: SELECT id FROM transactions WHERE event_id=p_event_id AND collector_id=auth.uid() → RETURN if found) + reconciler module under src/infrastructure/sync/ exposing replayPendingEvents(collectorId)/stopReplay/ReplayResult/classifyReplayError; single-in-flight via module-scope inFlightPromise ref; serial drain in listEvents timestamp-ASC + eventId tiebreak order (no Promise.all batching — order matters for member.* events; NFR-P6 budget met serially at ~300ms/event × 150 = 45s typical, half the 90s budget); per-event error classification: network/5xx=stop+backoff, unauthorized=stop, validation/not_found/unsupported_kind=skip+continue (event stays in queue with lastError for Story 8.5 retry UI); exponential backoff 10s → 600s cap mirroring SMS-worker NFR-R4 from Story 6.2; useReconciler hook at src/features/connectivity/api/useReconciler.ts subscribes window `online` event + mount-once boot replay + invalidateQueries(MEMBERS + MEMBER_PROFILE) on successful drain to swap Story 8.3's optimistic snapshots for server truth; mounted in AppLayout alongside ConnectivityIndicator; 12 reconciler vitest cases (empty queue / 3-events-succeed / mid-drain-TypeError / 5xx-network-stop / validation-skip-continue / unauthorized-stop / idempotent-replay / single-in-flight-shares-promise / stopReplay-mid-drain / unsupported_kind-skip / member.*-skip / 150-event happy-path); 4 useReconciler hook tests (collectorId-present-triggers / null-collector-no-call / online-event-triggers / unmount-removes-listener); 9 Deno contract tests (3 RPCs × insert + idempotent-hit + cross-collector); Playwright E2E flow-1-offline-replay (offline contribution → online → drain → audit-log assertion); NO new deps; bundle ≤ 3 KB gzipped target; memory hooks applied (Node 22 npm 10 + transactions_decrypted view update + supabase.rpc binding + BroadcastChannel test poll-with-deadline + DROP+CREATE for SQLSTATE 42P13 + db:migrate not db:reset); Story 8.1's hasFailed=false STAYS untouched (Story 8.5 surfaces ReplayResult.skipped as the hasFailed source); locks closure for Story 8.5 (stalled retry UI consumes skipped events) / 8.6 (offline read path reuses reconciler trigger)
  8-5-stalled-sync-alert: done  # merged 2026-05-15 via PR #71 (merge commit 1873e9a; commit edf8b08 [story]). Cross-LLM code-review 2026-05-15 (claude-sonnet-4-6, 3-layer adversarial) — 10 patch + 1 defer + 7 dismissed, all 10 patches batch-applied: retrySucceeded gated on networkFailures+skipped===0 (no false success on partial drain); retryResult/events reset on drawer close; @axe-core/playwright assertion added to the E2E (the false "no E2E uses axe-core/playwright" claim corrected — fixtures/axe.ts exists); resolveThresholdMs rejects a 0 override; recordedLabel guards loadedAt===0; drawer empty-state gated on events too; dropped outline-Fermer test re-added; retry-drains-queue test added; E6 threshold-timing fix rejected (breaks the test — useStalledSync reads threshold per effect run — the real page.unroute flake fixed via a mutable blockReplay route flag, E2E 3× green); useStalledSync initial-0 test assertion added. 1 defer: handleRetry mid-retry collector-change cancellation guard (deferred-work.md). Gates re-run green: typecheck / lint / 865 vitest / branches 76.13% / build / full Playwright suite (flow-8 + flow-1-offline-replay green; 2 unrelated local-env wrangler-worker failures). Implemented 2026-05-15 via bmad-dev-story on feat/8-5-stalled-sync-alert (Node 22/npm 10) — 8 tasks / 30 ACs; new useStalledSync hook (time-based NFR-P7 15-min detection, reload-durable per-collector localStorage marker safaricash:sync:stalled-since:{id}, all setStalled via setTimeout callbacks for react-hooks purity); useConnectivityState.hasFailed wired (Story 8.1 placeholder removed, deriveState untouched); ConnectivitySyncDrawer body filled (pending-ops list by member from listEvents + MEMBERS_QUERY_KEY cache, role=status amber stalled banner, "Retenter" CTA → replayPendingEvents with disabled+spinner in-flight, session-expired hint); ConnectivityIndicator pulse + spin motion-reduce guarded; 15 new connectivity.drawer.* i18n keys (placeholder_pending removed); Playwright flow-8-stalled-sync E2E with the safaricash:e2e:stalled-threshold-ms override seam. Gates: typecheck / lint --max-warnings=0 / 863 vitest passed (+19 vs 8.4 baseline 844) / branches 76.12% global (≥75%) + useStalledSync 93.75% isolated (≥85%) / build PWA 816.61 KiB / Playwright flow-8 + flow-1-offline-replay green (2 unrelated local-env worker failures: flow-3-cycle-settlement + receipt-url-worker — wrangler workers not running locally, identical to 8.4 local run). NO new deps / NO migration / NO Edge Function / NO src/domain change / NO OfflineEvent mutation. Spec drafted 2026-05-15 via bmad-create-story; FIFTH story of Epic 8 — Offline Resilience; makes the silent offline-write loop (Stories 8.2-8.4) visible + actionable on failure: time-based stalled detection (event pending > 15 min after reconnection, NFR-P7) via a new useStalledSync hook backed by a single reload-durable localStorage marker `safaricash:sync:stalled-since:{collectorId}` (the IndexedDB event log stays IMMUTABLE — no OfflineEvent field added); wires useConnectivityState's hasFailed placeholder (hardcoded false since Story 8.1) to the real stalled predicate (online && pendingCount>0 && now-stalledSince>=15min) so the pill escalates to the existing sync-failed state via the untouched deriveState; fills the ConnectivitySyncDrawer skeleton body with the pending-operations-by-member list (listEvents + member names from MEMBERS_QUERY_KEY cache) + a "Retenter" CTA that re-invokes Story 8.4's replayPendingEvents(collectorId) with the current auth session ("fresh context"); subtle amber pulse on the stalled pill (prefers-reduced-motion guarded); ConnectivitySyncDrawer `state` prop reinstated (Story 8.4 had removed it); stalled banner uses role=status NOT role=alert (UX "never a red alarm"); new connectivity.drawer.* i18n keys (stalled_banner / retry_cta / session_expired_hint / row kinds / member_fallback); STALLED_THRESHOLD_MS test-seam overridable via localStorage key `safaricash:e2e:stalled-threshold-ms` so the Playwright E2E need not wait 15 real minutes; ~3 unit test files (useStalledSync threshold+persistence with vi.useFakeTimers / useConnectivityState hasFailed+offline-priority / ConnectivitySyncDrawer empty+syncing+sync-failed+retry+session-expired) + Playwright stalled-sync E2E + axe; NO new deps / NO migration / NO Edge Function / NO src/domain change; bundle ≤ 3 KB gzipped; locks closure for Story 8.6 (offline read path — last Epic 8 story). 30 ACs / 8 tasks.
  8-6-offline-member-lookup-edit: done  # merged 2026-05-15 via PR #72 (merge commit fb8d6dc; commit e2c3ee1 [story]). Cross-LLM code-review 2026-05-15 (claude-sonnet-4-6, 3-layer adversarial) — 8 patch + 1 defer + 14 dismissed, all 8 patches batch-applied: useUpdateMember reuses offlineGuards isOfflineAtEntry/getCurrentCollectorId (AC #12 + Safari guard); added the AC #24 useMembers-serves-cache-offline test; added the member.deleted-still-skipped reconciler test (AC #27); E2E gained the one-member.updated-audit-row assertion + offline list+search step; onMutate patches the optimistic cache from PARSED values (schema trims/coerces); shouldPersistMemberQuery also requires data!==undefined; E2E last_event_id assertion uses toBeTruthy; providers.test.ts uses the QUERY_KEY constants. 1 defer: members.last_event_id single-slot idempotency marker correct only under drain-stop-on-IDB-failure (deferred-work.md). Gates re-run green: typecheck / lint / 887 vitest / branches 76.23% / build / full Playwright (flow-8-offline-member 2× + flow-8-stalled-sync + flow-1-offline-replay green; 2 unrelated local-env wrangler-worker failures). Implemented 2026-05-15 via bmad-dev-story on feat/8-6-offline-member-lookup-edit (Node 22/npm 10) — 9 tasks / 32 ACs; SIXTH and FINAL story of Epic 8. Offline read via TanStack Query persistence (@tanstack/react-query-persist-client + sync-storage-persister, localStorage key safaricash:query-cache, member queries only via shouldPersistMemberQuery dehydrate filter, 24h maxAge + buster, persisted cache dropped on sign-out alongside queryClient.clear); LocalDataNote ("Données locales — synchronisation en attente", role=status) on MemberList + MemberProfile when offline; useUpdateMember offline branch (networkMode:"always" + navigator.onLine check + TypeError/network fallback → member.updated event via new buildMemberUpdateEvent, optimistic MEMBERS+MEMBER_PROFILE cache patch/rollback, {wasOffline} return, offline_storage error code surfaced by MemberForm); edit route shows offline toast on wasOffline; reconciler resolveRpcName maps member.updated→update_member (member.created/deleted stay unsupported — out of scope); migration 0009 adds members.last_event_id + p_event_id to update_member with idempotent early-return (psql-smoke-tested: replay emits no 2nd audit row); update-member-idempotent Deno contract test registered in run-edge-tests.sh. Gates: typecheck / lint --max-warnings=0 / 884 vitest passed (+19 vs 8.5's 865) / branches 76.29% global (≥75%) / build PWA 824.08 KiB (+7.5 vs 8.5's 816.61 — 2 TanStack persist deps, within the ≤8KB story budget) / full Playwright suite (flow-8-offline-member + flow-8-stalled-sync + flow-1-offline-replay green; 2 unrelated local-env wrangler-worker failures). 2 new deps (TanStack persist packages — NOT a state lib). 1 migration + 1 Deno contract test. Scope: read+edit only — offline create/delete/import out of scope. Closes Epic 8 (6/6). Spec drafted 2026-05-15 via bmad-create-story; SIXTH and FINAL story of Epic 8 — Offline Resilience; extends the offline write loop (Stories 8.2-8.5) to the member surface so list/search/profile read AND member edit work offline (FR40 completeness). Two scoping decisions taken with the user: (1) offline reads served by TanStack Query PERSISTENCE (@tanstack/react-query-persist-client + sync-storage-persister, localStorage) — NOT the architecture's Service Worker NetworkFirst cache, because the SW is inactive in dev/E2E and SW-cached reads are untestable; persistence works identically in dev/prod/E2E and hydrates a cold offline start; (2) scope = read (list/search/profile) + EDIT (update) only — offline create/delete/import explicitly OUT of scope (member.created/member.deleted event types stay reserved for a Growth follow-up). Deliverables: PersistQueryClientProvider in providers.tsx persisting only the member query keys (buster + dehydrate filter); useUpdateMember gets a Story-8.3-style offline branch (networkMode:"always" + isOfflineAtEntry + member.updated event queued via a new buildMemberUpdateEvent + optimistic cache/rollback + offline_storage error + {wasOffline} return); reconciler resolveRpcName learns member.updated→update_member (member.created/deleted stay null/unsupported); migration adds members.last_event_id column + p_event_id to update_member with idempotent early-return (no double member.updated audit row on a reconciler retry — DROP+CREATE per SQLSTATE 42P13); Deno contract test update-member-idempotent registered in run-edge-tests.sh; LocalDataNote component shows "Données locales — synchronisation en attente" on list+profile when offline; new members.local_data_note i18n key. 2 new deps (first-party TanStack persist packages — NOT a state-mgmt lib). 1 migration + 1 Edge contract test. Bundle budget ≤ 8 KB gzipped (wider than 8.2-8.5's 3 KB due to the deps). 32 ACs / 9 tasks. Closes Epic 8 (6/6).
  epic-8-retrospective: optional

  # Epic 9 — Dashboard & Activity Visibility
  epic-9: in-progress
  9-1-dashboard-polled-stats: done  # merged 2026-05-16 via PR #73 (merge commit 5c8fd85; commit 5840d22 [story]). Cross-LLM code-review 2026-05-16 (claude-sonnet-4-6, 3-layer adversarial) — 6 patch + 0 defer + 8 dismissed, all 6 patches batch-applied: deriveDashboardStats sorts recentTransactions newest-first before the cap-5; the dashboard query key is date-stamped so a cold start across midnight uses a fresh key; dashboardTxRowSchema.amount → z.coerce.number() (PostgREST numeric-as-string, matches transactionRowSchema); staleTime dropped (refetchInterval owns the cadence); deriveDashboardStats gained a defensive now-param today-filter + a today-boundary test (AC #15); rounded-2xl → rounded-lg (project 16px token). Gates re-run green: typecheck / lint / 905 vitest / build PWA 828.90 KiB / full Playwright (flow-9-dashboard 2× + all green; 2 unrelated local-env wrangler-worker failures). Implemented 2026-05-15 via bmad-dev-story on feat/9-1-dashboard-polled-stats (Node 22/npm 10) — 8 tasks / 22 ACs; FIRST story of Epic 9. Pure deriveDashboardStats module (active-count / commission via the commission() domain fn / today's collection summing contribution+rattrapage, advances excluded, undone excluded by the transactions_decrypted view / recent-5) + useDashboardStats hook (refetchInterval 60_000 — architecture Q-ARCH6, no Realtime; composes the offline-persisted useMembers() + a ["dashboard","transactions"]-keyed query running 2 parallel transactions_decrypted fetches: today gte UTC-day-start + recent order-desc-limit-5; returns members + lastUpdatedAt). DashboardStatCards (3 numeric cards) + RecentActivity (5-row list, relative time off the lastUpdatedAt prop — no Date.now in render) components; /dashboard route rebuilt (CycleEndingAlert kept, LocalDataNote added). Persist filter shouldPersistMemberQuery renamed shouldPersistOfflineReadQuery + extended to ["dashboard",…] so the dashboard cold-starts offline. New dashboard.* i18n keys merged into the existing dashboard block. Gates: typecheck / lint --max-warnings=0 / 904 vitest passed (+17 vs 8.6's 887) / branches 75.8% global (≥75%) / build PWA 828.68 KiB (+4.6 vs 8.6 — within ≤5KB budget) / full Playwright suite (flow-9-dashboard 2× + flow-3-cycles-ending-alert no-regression + all green; 2 unrelated local-env wrangler-worker failures). NO new deps / NO migration / NO Edge Function / NO Realtime — client-side derivation. Spec drafted 2026-05-15 via bmad-create-story; FIRST story of Epic 9 — Dashboard & Activity Visibility. Replaces the /dashboard placeholder with the real morning-glance dashboard: 4 stats (active-members count, amount collected today FCFA, commission earned this cycle FCFA, 5 most recent transaction activities), refreshed every 60s via TanStack Query refetchInterval (architecture Q-ARCH6 — polling NOT Realtime), fully functional offline from the cached/persisted read-model. Active-count + commission derive from the existing already-offline-persisted useMembers(); commission via the commission() domain fn (INV-4 — 1 day's daily-amount/cycle). Today's-collection + recent-activity from a new ["dashboard",…]-keyed transactions_decrypted query persisted for cold-start offline by extending the Story 8.6 shouldPersistMemberQuery filter. Stat math is a pure separately-tested deriveDashboardStats module. NO new deps / NO migration / NO Edge Function / NO Realtime; client-side derivation (no server aggregation view at MVP). LocalDataNote (Story 8.6) reused for offline-freshness honesty. 22 ACs / 8 tasks. One scope decision flagged for review: "commission earned this cycle" = commission booked over the active cycle set (Σ commission(dailyAmount)), not a realised-only figure.
  9-2-cycles-ending-dashboard-alerts: done  # 2026-05-16 — closed as ALREADY-DELIVERED: the entire FR35 dashboard cycles-ending-alert surface was built by Story 3.5 (FR20). The three Story 9.2 acceptance criteria are satisfied verbatim by existing code: (1) "≥1 member with cycle ending in the window → alert banner with count + Voir CTA" → src/features/cycle/ui/CycleEndingAlert.tsx + src/features/cycle/api/useCyclesEndingAlert.ts (warning-palette banner, body_one/body_many count copy, "Voir" CTA, mounted on /dashboard); (2) "tap Voir → member list filtered to cycles-ending members" → <Link to="/members?filter=cycles-ending"> + MemberList's cycles-ending filter chip; (3) "tap × → dismissed for the session, reappears on next app open" → useCyclesEndingAlert's sessionStorage flag (sc_cycle_ending_alert_dismissed — survives a reload, cleared on tab close). The full happy path is E2E-tested by tests/e2e/flow-3-cycles-ending-alert.spec.ts (count / Voir→filtered list / ×→dismiss / persistence — green in CI). No implementation, no spec, no PR — FR20 (Epic 3) and FR35 (Epic 9) describe the same dashboard alert; Story 3.5 covered both. Decision taken with the user during bmad-create-story gap analysis.
  9-3-csv-export: backlog
  epic-9-retrospective: optional

  # Epic 10 — Saver Dispute Flow & Data Rights
  epic-10: backlog
  10-1-dispute-flag-surface: backlog
  10-2-dispute-notify-edge-function: backlog
  10-3-dispute-member-profile-banner: backlog
  10-4-saver-anonymisation-edge-function: backlog
  10-5-saver-optout-action: backlog
  epic-10-retrospective: optional
