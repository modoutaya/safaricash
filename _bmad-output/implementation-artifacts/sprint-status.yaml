# generated: 2026-04-19
# last_updated: 2026-05-14
# touched: 8-2-indexeddb-event-log → done (merged 2026-05-14 via PR #68, commits fc11bae [story] + e36763d [lockfile fix]; CI green on 2nd push — 1st push failed all 3 jobs at `npm ci` because local Node 24/npm 11 produced a lockfile that CI's Node 22/npm 10 rejected with "Missing: @esbuild/* from lock file" × 32 platform binaries — fix: nvm use 22 + rm node_modules + npm install + re-commit, lesson saved to memory `feedback_npm_lockfile_node_version.md`; implemented 2026-05-15 via bmad-dev-story on feat/8-2-indexeddb-event-log + cross-LLM code-review on sonnet-4-6 [3-layer adversarial: Blind Hunter / Edge Case Hunter / Acceptance Auditor] — verdict Changes requested, 3 HIGH + 8 MED + 4 LOW patches all applied in batch; SECOND story of Epic 8 — Offline Resilience; pure client-side infrastructure (NO UI / migration / Edge Function / i18n / state lib): `OfflineEvent` interface + `offlineEventSchema` Zod with `satisfies z.ZodType<OfflineEvent>` lockstep + `.refine(structuredClone)` payload guard (post-review) + `eventLog.ts` ~250 LOC under `src/infrastructure/sync/` exposing `appendEvent` / `listEvents` / `getEvent` / `countEvents` / `deleteEvent` + `openEventLogDb` singleton + test-only `_clearAllEvents` + `_resetEventLogDbForTests` (renamed for naming-convention parity, NOT exported from index.ts barrel post-review) + typed `OfflineEventLogError` with 5 codes (cause field redeclared as `override readonly cause?: unknown` for readonly contract preservation post-review); singleton IDB DB `safaricash_event_log` v1, store `events` keyPath `eventId` + autoIncrement:false explicit (post-review), non-unique composite index `byCollectorAndTime` on `[collectorId, timestamp]`; `openDB` options include `blocking()` callback (multi-tab v2 upgrade safety, post-review) + `terminated()` callback (forced-close recovery, post-review); `requireCollectorId` guard rejects empty/non-UUID inputs to listEvents/countEvents (post-review); `mapIdbError` cast tightened to `typeof` guard against primitive throws (post-review); `listEvents` returns rows sorted timestamp ASC then eventId ASC; `countEvents` O(log n) via `countFromIndex` + `IDBKeyRange.bound([collectorId, ""], [collectorId, "￿"])`; `deleteEvent` idempotent; 33 vitest cases (5 types + 28 eventLog; well above spec floor of 19) incl. round-trip + VALIDATION_FAILED + DUPLICATE_EVENT_ID + payload-structuredClone-refine reject + partition isolation + countEvents per partition + index-range correctness + cross-reload durability + cross-sign-out durability with vi.mock("@/infrastructure/supabase/client") running real `requestSignOut` + static-grep guard reading signOut.ts source asserting no _clearAllEvents/_resetEventLogDbForTests/@/infrastructure/sync imports + crash-mid-session recovery + deleteEvent idempotent + _clearAllEvents wipe + getEvent undefined on miss + Promise.all-3-appends + instanceof OfflineEventLogError + singleton memoisation + QUOTA_EXCEEDED + TRANSACTION_FAILED × 3 (get/count/clear) + re-throw-OEL-without-wrap + DB_OPEN_FAILED via `vi.doMock("idb")` + sort-tiebreak + 3 collectorId-guard cases (empty + non-UUID × 2); new deps `idb` ^8.0.3 (dep) + `fake-indexeddb` ^6.2.5 (devDep, polyfilled into vitest.setup.ts via `await import("fake-indexeddb/auto")`); cross-LLM dismissed findings: self-rescinded singleton reasoning + U+FFFD-vs-U+FFFF sentinel (verified U+FFFF at lines 146,177) + branch-coverage 81.25% defer already accurate + fake-indexeddb/auto intentional improvement; defer: schema.source accepting "online" deferred to Story 8.3 caller-side enforcement (filed in deferred-work.md § Story 8.2); gates: typecheck / lint (`--max-warnings=0`) / 783 vitest passed (+ 1 skipped; +41 vs Story 8.1 baseline of 742, of which 33 from this story + 8 from pre-existing tests that started passing after fake-indexeddb polyfill landed) / branches 76.4% global (≥ 75% gate) / `eventLog.ts` 97.26% stmts / 82.05% branches / 87.5% funcs / 97.1% lines isolated / build PWA precache 777.66 KiB unchanged (+0.19 KiB raw vs 8.1 baseline 777.47 KiB — idb tree-shakes aggressively); AC #22 82.05% vs claimed ≥ 85% — 5 defensive branches (DOMException narrow-cast + v2 schema-upgrade + eventId-tie return-0 + blocking() + terminated() callbacks all unreachable in jsdom) filed in `deferred-work.md` § Story 8.2 with revisit triggers (schema v2 bump OR Playwright real-browser); locks contract for Stories 8.3 (write-path + pendingCount real-source) / 8.4 (reconciler drains via listEvents → deleteEvent per success) / 8.5 (stalled-sync retry) / 8.6 (offline member lookup); Story 8.1's `useConnectivityState` pendingCount placeholder stays untouched until Story 8.3. Below: previous touched line:
# touched (previous): 8-1-connectivity-indicator → done (PR #67 merged 2026-05-15, commit fc80336; full CI green on FIRST try — pre-push memory from PR #66 lessons applied successfully; FIRST story of Epic 8 — Offline Resilience; useConnectivityState hook with navigator.onLine + window listeners + state derivation [offline→sync-failed→syncing→connected ordering] + pendingCount/hasFailed placeholders for Stories 8.3/8.4; ConnectivityIndicator pill (4 states with semantic colours per UX-DR5 "never red-alarm", aria-live + aria-label, 40px hit-area, animate-pulse on icon only for sync-failed); ConnectivitySyncDrawer skeleton (empty state + placeholder for Story 8.4 to populate, programmatic focus on close button); AppLayout integration with drawer state held at layout level; 9 i18n keys under new connectivity.* namespace; 24 vitest cases (6 hook + 11 pill + 7 drawer) incl. axe-clean × 4 states; lint catch on react-hooks/set-state-in-effect fixed; all local gates green (typecheck / lint / 742 vitest / 76.04% branches global / build PWA +5 KiB); cross-LLM review on sonnet-4-6 [Approve with suggestions, 0H/3M/3L] — all 6 patches applied: deriveState export + 5 priority tests (AC #17 gap closed), +3 i18n `*_idle` keys (NFR-L2 prep), removed phantom drawer state prop, toHaveBeenCalledTimes assertions, AC #19 deferral doc, hit-area regression test; 30/30 connectivity tests post-patches; Epic 8 kicked off. Below: previous touched line:
# touched (previous): 8-1-connectivity-indicator → ready-for-dev (spec drafted via bmad-create-story; FIRST story of Epic 8 — Offline Resilience; persistent header pill in AppLayout with 4 states (connected/syncing/offline/sync-failed) via navigator.onLine + pendingCount placeholder ; `useConnectivityState` hook contract locked for Stories 8.3/8.4 to wire real IndexedDB-backed count + reconciler failure flag; `ConnectivitySyncDrawer` skeleton with "Aucune opération en attente" placeholder (Story 8.4 populates with pending list + retry CTAs); 9 i18n keys under new connectivity.* namespace; ~18-22 vitest cases incl. axe-clean × 4 states; no migration, no Edge Function, no schema change — purely frontend surface that subsequent Epic 8 stories populate); kicks off Epic 8. Below is Epic 7's history:
# touched (previous): epic-7 → done (5/5 stories complete: 7.1→7.5 all merged 2026-05-13→2026-05-15); 7-5-cycle-settled-final-sms → done (PR #66 merged 2026-05-15, commit b1c3f99 + 2 CI-fix commits; CLOSING story of Epic 7; cross-LLM review intercepted a CI-breaking SMS length bug (template was 162-167 chars worst-case, fixed by removing 'Merci. ' + capping firstName 16→9 → exactly 160 chars at worst case) + completed AC #11 (Worker E2E settlement page) which was previously checked-but-unimplemented + completed AC #9 with 2 missing Deno contract cases (unaccent + single-token); the LIGHTEST + LAST story of Epic 7 — 2 migrations [format_sms_body settlement branch rewrite (firstName + DD/MM cycle range + closing statement + plain-digit amount) + get_receipt_payload extension with cycle_start_date + cycle_end_date, DROP+CREATE workaround for SQLSTATE 42P13]; Worker render.ts settlement branch (renderSettlementReceiptHtml hides projected balance + cycle day + dispute CTA, shows cycle period DD/MM/YYYY + closing statement, .settlement-closing CSS class, defensive missing-dates fallback); 13 new Worker render tests + updated format_sms_body contract test + new length-boundary case + new get_receipt_payload cycle dates contract test; Playwright Flow 3 SMS body regex assertions; SMS ≤ 160 chars worst-case (GSM-7 discipline preserved); all local gates green (typecheck / lint / 720 vitest / 44 Worker / 76.32% branches global / build, Deno + Playwright deferred to CI); when 7.5 merges → Epic 7 transitions to done [5/5 stories complete]. Below was Story 7.4's touched line, preserved for history: 7-4-settlement-reauth-gate → done (PR #65 merged 2026-05-14, commit 010e955; full CI green including 9 Deno contract tests + Playwright Flow 3 E2E + 5 migrations on FIRST TRY; the LARGEST story of Epic 7 (implemented 2026-05-14 via bmad-dev-story + code-review patches; LARGEST story of Epic 7 — end-to-end commit pipeline shipped; 5 migrations [+1 vs spec: reject_transaction_allow_settlement.sql to relax Story 3.4 trigger for kind='settlement' on completed cycles] + commit_cycle_settlement SECURITY DEFINER RPC ~80 LOC with FOR UPDATE lock + NFR-R3 server-side payout recompute + cross-check; cycle-settlement Edge Function ~270 LOC mirroring Story 6.6 pattern with verifyPassword shared helper + P0002 error-message-prefix mapping to RFC 7807 [+2 keys in KNOWN_PROBLEMS: payout_mismatch + cycle_not_settleable]; @/features/settlement folder with useCommitSettlement (10-code classifyError + invalidation + this-binding compliant) + commitSettlementError typed class + SettlementReauthDialog (native <dialog>, form-wrap, password-trim — Story 6.6 P3/P8 preserved); route handler swap (Story 7.3 toast.info stub → dialog opens → in-route view-swap to <EnvelopeHandoverScreen> with payoutAmount=settled_payout); 14 i18n keys under new settlement.reauth.* + settlement.toast.* sub-namespaces; existing audit_emit_cycle_transitioned trigger auto-emits cycle.settled on cycle UPDATE — no audit allowlist change; existing format_sms_body('settlement', tx_id) template reused via synthetic transactions.kind='settlement' row + extended trigger CASE; 46 new vitest cases (17 hook + 11 dialog + 18 route) + 8 Deno RPC contract cases + 1 Playwright Flow 3 E2E (happy + wrong-password retry + service-role side-effects); deviation documented: spec AC #13 Edge Function HTTP contract test deferred to Playwright (matches Stories 6.6 / 6.7 pattern); all local gates green (typecheck / lint / 705 vitest / 76.32% branches global / build, +8 KiB precache); Deno + Playwright deferred to CI (no Deno binary locally); settlement route at 96.55/88.23/90.9/96.49; useCommitSettlement at 98/82.22/100/98; SettlementReauthDialog at 90.38/82.35/90/97.87; cross-LLM code-review on sonnet-4-6 [Changes requested → all 5 patches applied: HIGH isSubmitting fix via dialog→route onMutatingChange callback, MED cycle/member-mismatch → 400 request_invalid, MED server_payout in RFC 7807 body, LOW axe inline-error state, LOW Deno auth.uid()=null case]; gates re-run green; blocks Story 7.5 (final SMS content tweaks + receipt URL routing for kind='settlement')
# project: SafariCash
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-04-19
last_updated: 2026-05-14
project: SafariCash
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # Epic 1 — Collector Onboarding & Sign-In
  epic-1: in-progress
  1-1-project-bootstrap: done
  1-2-supabase-foundation: done
  1-3-reauth-edge-function: done
  1-4-rate-limit-middleware: done
  1-5-phone-otp-signin: done  # superseded by 1-5b (PRD v1.3 auth pivot — Termii KYC blocker)
  1-5b-password-auth-switch: review  # rewrites OTP login → signInWithPassword + decommissions auth-sms-hook + check_collector_registered RPC; also rewrites Story 1.3 re-auth Edge Function to password verification
  1-6-session-management: done
  1-7-sign-out: done
  1-8-ci-pipeline-gates: done
  epic-1-retrospective: optional

  # Epic 2 — Member Lifecycle Management
  epic-2: in-progress
  2-1-member-list-search: review
  2-2-create-member-manual: review  # spec generated 2026-04-21 via bmad-create-story; React Hook Form v7 first wiring + atomic create_member_with_cycle RPC
  2-3-contacts-bulk-import: review  # spec generated 2026-04-22; reuses Story 2.2 RPC via p_created_via='contacts_import' + new phone-hash unique constraint migration; 3-step state machine + Promise.allSettled with 5-concurrency limiter; Chromium-Android only (iOS fallback to manual entry)
  2-4-member-profile-view: done  # PR #11 merged 2026-04-22; 19 new tests; full gates green (336 vitest, coverage 75.7% > 75%); E2E env-gated
  2-5-edit-member-impact-alert: done  # PR #13 merged 2026-04-23; 17 new tests; uncovered + fixed system-wide audit-trigger JWT bug (migration 0017)
  2-6-delete-member-confirmation: done  # PR #17 merged 2026-04-23; 18 new tests; uncovered + fixed CI edge-runtime gap; LAST story of Epic 2
  2-7-restart-cycle: done  # PR #15 merged 2026-04-23; 14 new tests; full gates green; CI passed first-try thanks to local Playwright validation
  epic-2-retrospective: optional

  # Epic 3 — Cycle Engine & Progression
  epic-3: in-progress  # kicked off 2026-04-23 with Story 3.1 (cycle-invariants ADR)
  3-1-cycle-invariants-adr: done  # PR #20 merged 2026-04-23; ADR-004 ships 8 invariants with property test skeleton names
  3-2-cycle-engine-pure-module: done  # PR #22 merged 2026-04-24; cycle module 100% coverage; fast-check installed; computeMemberStats moved to domain
  3-3-cycle-status-transitions: done  # PR #24 merged 2026-04-24; promote_cycle_on_advance trigger + cycle.transitioned audit event
  3-4-prevent-post-completion-contributions: done  # PR #26 merged 2026-04-24; FR19 DB enforcement + isCycleClosedForTransactions helper
  3-5-cycles-ending-alerts: done  # PR #40 review complete — 8 patches addressed (setSearchParams scope, E2E UTC date, body_zero key, hook test QueryClientProvider, dismiss-chip a11y, aria-live keep-mounted, useCallback dismiss, dismiss-disappears test); 2 deferred items in deferred-work.md (useMembers isError handling for Story 9.1, Tailwind warning-palette token cleanup); full gates green; 20/20 Playwright validated locally; closes Epic 3
  epic-3-retrospective: optional

  # Epic 4 — Daily Transaction Capture (Core)
  epic-4: in-progress  # kicked off 2026-04-24 with Story 4.1
  4-1-member-action-sheet-component: done  # PR #28 merged 2026-04-24; MemberActionSheet + MemberList rewire
  4-2-progressive-toast-component: done  # PR #30 merged 2026-04-24; pure component shipped
  4-3-record-contribution-online: review  # implemented 2026-04-24/25; full Flow 1 online path; 441 vitest, 29 edge, 19 Playwright validated locally
  4-4-record-rattrapage: review  # implemented 2026-04-26 via bmad-dev-story; 2 migrations (days_covered + cross-kind CHECK + record_rattrapage RPC) + useRecordRattrapage hook + ProgressiveToast bodyOverride extension + showRattrapageToast + action-sheet long-press (500ms) + inline × 2/× 3/× 4 jours grid + MemberList wiring + 8 Deno contract tests + new Playwright spec; closed-cycle gate inherited from Story 3.4; SMS enqueue inherited from Story 4.3; full gates green
  4-5-undo-transaction-window: review  # implemented 2026-04-26 via bmad-dev-story; rewrites 4.3 hard-DELETE → soft-undo; 4 migrations + undo_transaction RPC + UndoTransactionError typed class + MemberList typed-error toast handling + 7 Deno contract tests + 8 vitest cases; closes Epic 4; full gates green; dropped spec AC #1's undone_event_id column (would have polluted audit chain via second UPDATE)
  epic-4-retrospective: optional

  # Epic 5 — Emergency Advance Flow
  epic-5: in-progress  # kicked off 2026-04-26 with Story 5.1 (AdvanceSimulationPanel)
  5-1-advance-simulation-panel: review  # implemented 2026-04-27 via bmad-dev-story; pure component AdvanceSimulationPanel.tsx (4-row card driven by Story 3.2 cycle-engine primitives, 3 states empty/valid/over-limit, aria-live=polite on row 4 only, currency formatted via formatFcfaAmount, all colours via Tailwind tokens); 8 vitest tests incl. jest-axe across 3 states; 7 i18n keys under advance.simulation.*; no E2E (deferred to 5.4 commit-path)
  5-2-advance-situation-context: review  # implemented 2026-04-27 via bmad-dev-story; AdvanceFlow screen at /members/:id/advance (situation panel via Story 2.4 useMemberProfile + 3 suggested-amount chips ADVANCE_SUGGESTED_AMOUNTS=[50k,100k,150k] + free-form numeric input + Story 5.1 simulation panel); MemberList onAdvance wires navigation; closed-cycle / no-active-cycle / not-found redirect defensively; CTA disabled (Story 5.3 enables, 5.4 commits); 11 AdvanceFlow tests + 1 MemberList integration; jest-axe clean
  5-3-motive-saver-acknowledgment: review  # implemented 2026-04-27 via bmad-dev-story; AdvanceFlow extended with motive textarea (≥3 chars trimmed) + saver-ack checkbox (NOT pre-checked, BDD ligne 930 copy verbatim) + precedence-ordered CTA gate (amount→motive→ack) with contextual disabled tooltip; AdvanceConfirmPayload extended to {amount, motive, acknowledged}; 11 new test cases; all gates green
  5-4-commit-advance-transaction: review  # implemented 2026-04-27 via bmad-dev-story; closes Epic 5; 2 migrations (motive+saver_acknowledged columns + cross-kind CHECK + record_advance SECURITY DEFINER RPC with server-side capacity check) + RecordAdvanceInputSchema (Zod with z.literal(true) for ack) + useRecordAdvance hook (9 typed error codes) + showAdvanceToast (bodyOverride) + route wiring of onConfirm with typed-error toast.error mapping; 8 Deno contract tests incl. DB CHECK + audit-payload integrity; new flow-2-advance.spec.ts E2E; triggers fire for free (Story 3.4 closed-cycle gate, Stories 1.2/3.3/4.5 audit, Story 4.3 SMS enqueue, Story 3.3 promote_cycle_on_advance + cycle.transitioned)
  epic-5-retrospective: optional

  # Epic 6 — Saver Trust Communications
  epic-6: in-progress  # kicked off 2026-04-27 with Story 6.1 (sms-dispatch infrastructure)
  6-1-sms-dispatch-edge-function: review  # implemented 2026-04-27 via bmad-dev-story; 3 migrations (schema extension + trigger rewrite + audit_append_external SECURITY DEFINER helper) + sms-dispatch Edge Function (POST, JWT-auth, RLS-respecting tx lookup, RFC 7807, hashed-phone structured logging) + 5 trigger contract tests + 8 Edge Function contract tests; opens Epic 6; full gates green (548 vitest / 65 edge / build / typecheck / lint)
  6-2-sms-worker-termii-retry: review  # implemented 2026-04-28 via bmad-dev-story; 3 migrations (audit allowlist 1-line extension + pg_cron 30s schedule + claim_sms_queue_batch RPC + audit_append_external 5-arg overload) + sms-worker Edge Function (POST + service-role-key gate + Promise.all batch parallelisation + FOR UPDATE SKIP LOCKED drain + backoff 10s→600s cap + 24h abandon + soft-undo skip via undo_transaction) + sendSmsNoRetry termii client extension + pure backoff module 100% covered + 4 new contract test files (8 backoff + 12 worker + 6 allowlist + 5 cron); shared test-fixtures extracted from Story 6.1 tests; full gates green (548 vitest / 94 edge / build / typecheck / lint)

  6-3-sms-copy-templates: review  # implemented 2026-04-28 via bmad-dev-story; 3 migrations (receipt_token 128-bit + unique idx + CHECK regex / format_sms_body SECURITY DEFINER helper with 4 template branches + unaccent + receipt URL via current_setting GUC fallback / trigger replacement 3-line diff vs Story 6.1) + 3 contract test files (8 format-helper + 1 banking-language + 1 length/ASCII) + 1 regression assertion in Story 6.1 trigger test; full gates green (548 vitest / 104 edge / build / typecheck / lint)

  6-4-receipt-url-worker: review  # implemented 2026-04-28 via bmad-dev-story; Cloudflare Worker safaricash-receipt-url (GET /r/{token} no-JS HTML + inline CSS + token regex defence + service-role lookup) + get_receipt_payload SECURITY DEFINER RPC (Story 4.5 handshake — undone rows 404) + dispute.ts 501-stub (Story 10.2 wires) + 6 token + 22 render unit tests incl. jest-axe Level A + XSS escape defence + 7 Playwright cases + CI wrangler-dev step on port 8788; full gates green (576 vitest / typecheck / lint / build)

  6-5-first-sms-consent-optout: review  # implemented 2026-04-28 via bmad-dev-story; 6 migrations (sms_opt_out columns + partial idx / trigger replacement / audit allowlist 1-line ext / set_member_sms_opt_out RPC / get_member_id_from_token RPC / find_members_by_phone RPC) + sms-inbound Edge Function (Termii STOP webhook with ?secret= gate + multi-collector opt-out) + receipt-url Worker GET/POST /r/{token}/opt-out routes + render module additions; full gates green (584 vitest / 121 edge / build / typecheck / lint)

  6-6-resend-cycle-history: done  # implemented 2026-05-12 via bmad-dev-story + code-review patches applied; 4 migrations (0050 template_key 'resend' / 0051 audit allowlist 'sms.resend_initiated' / 0052 format_resend_sms_body wrapper [+ auth.uid ownership check] / 0053 enqueue_resend_history RPC [+ staggered created_at via row_number()] with Story 4.5 undone-filter + Story 6.5 opt-out short-circuit) + _shared/verify-password.ts (extracted from Story 1.5b re-auth, second consumer landed) + sms-resend-history Edge Function (POST + JWT + RFC 7807 + structured logging, error.message redacted from 500 detail, password trim+min1, NaN guard) + ResendHistoryDialog (form-wrapped for Enter-to-submit) + useResendHistory hook (typed ResendHistoryError + MEMBER_PROFILE_QUERY_KEY invalidation [AC #12 fix] + TypeError class-identity for network detection) + member profile button + 16 i18n keys + 3 Deno contract tests (URN-exact assertions, not substring) + audit-allowlist regression + Playwright E2E; 21 ACs + 14 tasks; gates: typecheck/lint/598-vitest/build all green; 8 inherited issues deferred

  6-7-per-transaction-receipt-share: done  # implemented 2026-05-12 via bmad-dev-story + code-review patches; 2 migrations (0054 transactions_decrypted exposes receipt_token / 0055 enqueue_resend_transaction RPC with uniform P0002 + auth.uid ownership + opt-out + no-phone + undone + unsupported_kind short-circuits) + Deno contract test (8 cases incl. unsupported_kind defence) + shareReceipt helper (Web Share API + clipboard fallback, never logs token, NBSP intentional) + ResendTransactionError + useResendTransaction hook (TanStack invalidation per 6.6-review pattern + TypeError class-identity) + TransactionReceiptSheet (native <dialog>) + interactive MemberProfile rows + 22 i18n keys + Playwright E2E + 4 missing review-required tests added (AC #17 receipt_token assert + AC #21 interactive-row + AC #22 tap-opens-sheet); NO Edge Function (direct RPC, per-tx not in FR5 list); all local gates green (typecheck / lint / 624 vitest / build); 5 inherited issues deferred
  6-8-whatsapp-secondary-delivery: backlog
  epic-6-retrospective: optional

  # Epic 7 — Cycle Settlement Ceremony
  epic-7: done  # COMPLETE — 5/5 stories merged 2026-05-13→2026-05-15: 7.1 SettlementSummaryCard (PR #61), 7.2 EnvelopeHandoverScreen (PR #62), 7.3 Settlement route + entry CTA (PR #63), 7.4 Atomic commit + re-auth gate (PR #65 — the largest), 7.5 Settlement SMS + receipt page (PR #66). Epic 7 ships the full day-30 cycle-settlement ceremony: collector taps "Clôturer le cycle" → SettlementSummaryCard with NFR-R3 zero-tolerance payout from Story 3.2 settle() → password re-auth dialog → cycle-settlement Edge Function → atomic SECURITY DEFINER RPC (FOR UPDATE lock + server-side payout cross-check + INSERT synthetic kind='settlement' transaction + UPDATE cycle.status='settled' firing existing cycle.settled audit emit) → frontend view-swap to EnvelopeHandoverScreen → saver receives settlement SMS with firstName + cycle period + receipt URL → settlement receipt page (Cloudflare Worker) shows cycle period + closing statement, hides dispute CTA (settlement is irreversible).
  7-1-settlement-summary-card: done  # merged 2026-05-13 via PR #61 (commit e4f2713); full CI green incl. cross-LLM code-review patches; pure presentation atom for Epic 7 ceremony surface — math via Story 3.2 settle()/commission() (NFR-R3 compliance), aria-live on row 4 only, 11 i18n keys under new settlement.summary.* namespace, 13 vitest+RTL+jest-axe cases; unwired until Story 7.3 (route) and 7.4 (re-auth + commit RPC)
  7-2-envelope-handover-screen: done  # merged 2026-05-14 via PR #62 (commit 030f13a); full CI green incl. cross-LLM code-review patches; pure presentation atom — Epic 7 emotional climax (UX-DR10 "moment of crystallised trust"); 5-block anatomy with Check icon circle / h2 / inline-styled amount body / 3-config subtext live-region / single CTA; mount-time programmatic focus on CTA (jsx-a11y/no-autofocus compliant); 6 i18n keys (body split _amount_prefix + _recipient documented in fr.json._notes for Story 1.5 translators); 12 vitest+RTL+jest-axe cases; Pride-over-playfulness — only essential animate-spin on Loader2 when pending; consumed by Story 7.4 post-commit
  7-3-settlement-initiation-computation: done  # merged 2026-05-14 via PR #63 (commit 19f6a25); full CI green incl. cross-LLM code-review patches; route /members/:id/settlement mounts Story 7.1 SettlementSummaryCard with real data via useMemberProfile; UUID guard + 4 preconditions (active/settled/with_advance/null cycle → redirect to profile); "Clôturer le cycle" CTA in profile header gated on currentCycle.status === "completed"; advances sorted newest-first; onConfirm stub is toast.info with enriched Story 7.4 TODO checklist (swap handler + drive isSubmitting + post-commit envelope nav); NFR-R3 zero-tolerance preserved (route never recomputes — card calls settle() internally); ready for Story 7.4 to swap onConfirm handler
  7-4-settlement-reauth-gate: done  # merged 2026-05-14 via PR #65 (commit 010e955); full CI green INCLUDING 9 Deno contract tests + Playwright Flow 3 E2E + 5 migrations on first try; the LARGEST story of Epic 7 — end-to-end settlement commit pipeline: commit_cycle_settlement SECURITY DEFINER RPC + cycle-settlement Edge Function (verifyPassword shared helper) + @/features/settlement folder (useCommitSettlement hook + commitSettlementError typed class + SettlementReauthDialog native <dialog>); route Story 7.3 stub replaced with dialog → in-route view-swap to <EnvelopeHandoverScreen>; NFR-R3 zero-tolerance preserved at 2 layers (client settle() + server recompute + cross-check); existing audit_emit_cycle_transitioned trigger emits cycle.settled automatically; existing format_sms_body('settlement', tx_id) template reused via synthetic transactions.kind='settlement'; cross-LLM code-review on sonnet-4-6 [Changes requested → all 5 patches applied incl. HIGH isSubmitting bug fix]; blocks Story 7.5 (final SMS content tweaks + receipt URL routing for kind='settlement')
  7-5-cycle-settled-final-sms: done  # merged 2026-05-15 via PR #66 (commit b1c3f99 + 2 CI-fix commits 092c9c9 + d44740b); CLOSING story of Epic 7; 2 migrations (format_sms_body settlement-branch rewrite with firstName + DD/MM cycle range + amount, GSM-7 single-SMS 160-char discipline via removed Merci. + firstName cap 9; get_receipt_payload extended with cycle_start_date + cycle_end_date via DROP+CREATE workaround for SQLSTATE 42P13) + Worker render.ts settlement branch (renderSettlementReceiptHtml: "Cycle clôturé" title + period row + closing statement + hide dispute CTA + keep opt-out, .settlement-closing CSS); 13 Worker render tests + 4 Deno format_sms_body settlement cases (incl. unaccent + single-token + length boundary) + 1 Deno get_receipt_payload contract test + Worker E2E settlement page + Playwright Flow 3 SMS body assertions; cross-LLM code-review on sonnet-4-6 intercepted the SMS length bug pre-CI [1 HIGH + 2 MED + 3 LOW, all applied]; 2 CI fails surfaced: (a) hardcoded fixture-derived dates from Playwright fixture mistakenly used in Deno test, (b) wiring of dormant Story 7.4 contract test exposed stale "Cycle clos." assertion broken by 7.5's template change — both fixed, gain: 9 dormant 7.4 tests now in CI
  epic-7-retrospective: optional

  # Epic 8 — Offline Resilience
  epic-8: in-progress
  8-1-connectivity-indicator: done  # merged 2026-05-15 via PR #67 (commit fc80336); FIRST story of Epic 8 — kicks off Offline Resilience; full CI green on FIRST try (pre-push memory from PR #66 lessons applied successfully); pure frontend story: useConnectivityState hook + ConnectivityIndicator pill (4 states, UX-DR5 never-red-alarm, aria-live + aria-label, 40px hit-area) + ConnectivitySyncDrawer skeleton (Story 8.4 will populate) + AppLayout integration + 12 i18n keys; cross-LLM code-review [Approve with suggestions, 0H/3M/3L — 6 patches applied incl. deriveState export + 5 priority tests, *_idle i18n keys for NFR-L2 prep, phantom prop cleanup]; 30 vitest cases incl. axe-clean × 4 states; all gates green (typecheck / lint / 76.04% branches global / build PWA +5 KiB)
  8-2-indexeddb-event-log: done  # merged 2026-05-14 via PR #68 (commits fc11bae + e36763d lockfile-fix); CI green on 2nd push (1st CI failed at `npm ci` because Node 24/npm 11 locally produces a lockfile incompatible with Node 22/npm 10 in CI — fix: nvm use 22 + regenerate lockfile — lesson saved to memory `feedback_npm_lockfile_node_version.md`); implemented 2026-05-15 via bmad-dev-story + cross-LLM code-review on sonnet-4-6 (3-layer adversarial) all patches applied; SECOND story of Epic 8 — Offline Resilience; pure client-side infrastructure (no UI, no migration, no Edge Function): `OfflineEvent` type + Zod schema (incl. `payload.refine(structuredClone)` post-review) + `eventLog` module under `src/infrastructure/sync/` (singleton IDB DB `safaricash_event_log` v1, composite index `(collectorId, timestamp)`, append-only via `keyPath: eventId`); CRUD = appendEvent / listEvents / getEvent / countEvents / deleteEvent + test helpers (NOT re-exported from barrel post-review); `requireCollectorId` guard rejects empty/non-UUID inputs; `blocking()` + `terminated()` callbacks on openDB for multi-tab v2 safety + storage-eviction recovery; typed `OfflineEventLogError` with 5 codes (cause field is `override readonly` post-review); 33 vitest cases (5 types + 28 eventLog; spec floor was 19) incl. crash-durability + cross-reload + cross-sign-out with vi.mock auth-client + static-grep guard on signOut.ts + cross-collector partition + concurrent appends + DB_OPEN_FAILED via vi.doMock + rare-error mapping + sort tiebreak + 3 collectorId-guard cases + 1 payload structuredClone refine; new deps `idb` ^8.0.3 (dep) + `fake-indexeddb` ^6.2.5 (devDep); gates green (typecheck / lint / 783 vitest / 76.4% branches global / build PWA precache 777.66 KiB); AC #22 isolated branches 82.05% vs claimed ≥ 85% — 5 genuinely-defensive branches (DOMException narrow-cast / v2 schema-upgrade / eventId-tie return-0 / `blocking()` / `terminated()` callbacks) filed in deferred-work.md § Story 8.2 (revisit triggers: schema v2 bump OR Playwright real-browser); cross-LLM review verdict was Changes requested with 3 HIGH (test race / blocking callback / AC #14 sign-out test) + 8 MED + 4 LOW + 1 defer (`source: "online"` permissive in schema, Story 8.3 enforces) + 4 dismissed (self-rescinded singleton / U+FFFD-vs-U+FFFF sentinel verified U+FFFF / branch coverage defer accurate / fake-indexeddb/auto intentional improvement); locks contract for Stories 8.3 (write-path + pendingCount real-source) / 8.4 (reconciler) / 8.5 (stalled-sync retry) / 8.6 (offline member ops)
  8-3-outbox-pattern-queue: backlog
  8-4-reconciler-replay: backlog
  8-5-stalled-sync-alert: backlog
  8-6-offline-member-lookup-edit: backlog
  epic-8-retrospective: optional

  # Epic 9 — Dashboard & Activity Visibility
  epic-9: backlog
  9-1-dashboard-polled-stats: backlog
  9-2-cycles-ending-dashboard-alerts: backlog
  9-3-csv-export: backlog
  epic-9-retrospective: optional

  # Epic 10 — Saver Dispute Flow & Data Rights
  epic-10: backlog
  10-1-dispute-flag-surface: backlog
  10-2-dispute-notify-edge-function: backlog
  10-3-dispute-member-profile-banner: backlog
  10-4-saver-anonymisation-edge-function: backlog
  10-5-saver-optout-action: backlog
  epic-10-retrospective: optional
